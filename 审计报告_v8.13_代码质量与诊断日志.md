# 审计报告 v8.13：代码质量全景审计 + 结构化诊断日志层

> 时间：2026-09（v8.12 之后，v8.13 轮）
> 方法：6 个模块簇并行深读（约 1.1 万行 / 50 文件）+ 交叉 grep 调用关系 + 生产日志证据对照（443s 综述案例）+ 检索数值实机验证（BM25 倒排逐位等价、缓存往返无损）。
> 目的：①全面审计（算法缺陷/模块冲突/屎山/补丁痕迹/性能）②落地结构化诊断日志（diag 层），供后续真实测试反馈定位。
> 本文档与《后续计划_优化与论文实验.md》互补：后者管「产品优化/论文实验」，本文管「代码健康/修复路线」。

---

## 一、结论摘要（TL;DR）

- 整体工程化程度**中上**：引用完整性、发布门、断点续传、检索代码级治理（去重/预算）等关键路径已有确定性校验，122+ 测试全绿。
- 三类系统性欠账：**①观测体系五套机制职责纠缠**（business/feedback/retrieval/SSE/cache 各写各的）；**②请求级状态散落全局变量**（并发互踩，3 个 P1）；**③写作链 1419 行 god module + 魔数 + 复制粘贴块**（max_per_section 公式 3 处、skill 截断 2 处、收尾 prompt 3 份）。
- 本轮已落地：**diag 结构化诊断层**（16 类事件、14 处埋点）+ **8 项低风险修复**（含 1 项 v8.12 自我引入的回归、3 项路径逃逸安全漏洞、1 项观测事件恒丢失）。
- **红线**：BM25+ 公式偏离与 e5 前缀策略失配两项检索 P1，直接动检索口径，**必须等 questions.jsonl + gold_dois 到位（离线评测基线）后再修**。

---

## 二、v8.13 已落地

### 2.1 诊断日志层（diag）

| 项 | 内容 |
|---|---|
| 模块 | `agent/src/core/diag.py`（新增） |
| API | `diag(event, **fields)` 单事件；`diag_span(event, **fields)` 计时区间（start/done/error 自动 dur_ms） |
| 双 sink | `logs/diag/diag_YYYY-MM-DD.jsonl`（结构化，可 grep/jq/脚本聚合）+ `agent.log` INFO 行（req= 串线） |
| 关联字段 | 自动注入 req/session/job（复用 tracing.py contextvars） |
| 安全 | 永不抛异常；单字段截断 500 字符；per-date 句柄缓存 + 线程锁；跟随 CITRUS_LOG_DIR（测试独立 sink） |

**事件清单（16 类）**：request_start/done/error、llm_usage（覆盖全部 LLM 调用点）、llm_call（agent 主循环+收尾）、agent_done、supervisor_done、write_route、classify、pipeline_start、plan（含失败）、section、section_condensed、retrieval（空结果归因）、retrieval_stages（embed/vector/bm25/rerank 分阶段 ms）、unify、prune、ref_verify、publish。

**后续真实测试后**：把 `logs/diag/*.jsonl` 直接发给本 Agent 即可逐阶段定位（无需再手工贴长日志）。

### 2.2 修复批（8 项，全部有测试锁定）

| # | 问题 | 修复 |
|---|---|---|
| A2 | **v8.12 自我引入回归**：expert_graph 传 cls 时漏传 llm → classify 的 LLM 结构化兜底在生产路径变死代码（纯正则路由，误判无纠偏） | expert_graph 补传进程级复用同步 client（`_get_sync_cls_llm`） |
| F1 | supervisor_done 业务事件**恒 NameError 被吞**（citation_info 在 blog 之后才赋值）→ 该事件从未成功写出 | citation_info 计算上移 + 补 diag 事件 |
| A-01 | readfile 相对路径分支无白名单校验 → `../../` 读取任意文件 | 相对路径分支同样 `_is_path_allowed` |
| A-10 | pdf_read 相对路径落回 CWD 解析 → 逃逸读任意 PDF | 相对路径一律锚定 workspace/ + is_relative_to |
| A-11 | file_ops `startswith` 前缀漏洞（output_evil 可绕过）+ OFFLOAD 目录 CWD 相对 | is_relative_to 严格判定 |
| A-07 | FILE_WRITE_MAX_SIZE_MB 死配置（定义无消费） | 写入前大小检查（append 按合并后总长） |
| A1 | 悬空引用（正文[n]无条目）仍发布进正式文件 | 发布门禁拦截"无对应文献条目"类 issue，保留草稿 |
| F8 | 检索三档计时变量（embed/vector/bm25）算完即弃 | 接入 diag `retrieval_stages` 事件 |
| API-1 | workspace-files 端点 `startswith` 前缀校验可列 output 兄弟目录文件 | is_relative_to 严格判定 |
| API-2 | `/workspace` 整目录静态暴露（input/临时文件 HTTP 可达，前端实际只用 output/） | 挂载收窄为 `/workspace/output`（URL 兼容不变） |

新增测试：`tests/test_diag.py`（3 项）、`tests/test_security_fixes.py`（4 项）、发布门禁集成测试 1 项。

---

## 三、P1 真缺陷清单（17 条）

> 状态：✅=本轮已修；📋=待修（附精确方案）；🔒=需数据集验证后再动（检索口径红线）。

### 3.1 已修（见 §2.2）

### 3.2 待修（按建议顺序）

| # | 簇 | 问题 | 精确修复方案 |
|---|---|---|---|
| F3/A3 | 写作 | 断点续传状态机三处不一致：①claim 置 `resumed` 后崩溃/取消永久卡死（cleanup 只清 running）②`partial`（缺章）任务不可续 ③全量续传 chapters=0 时跳过 unify/publish → 草稿永不发布但被标 done | ①pipeline 外层 try/finally `finish_task("aborted")` ②cleanup 覆盖 resumed+partial ③chapters==0 且草稿存在时直接走发布 |
| F2 | 图编排 | `_classify_document` 同步 OpenAI 阻塞事件循环（每次新建 client） | `await asyncio.to_thread(...)` + 走 llm_pool FAST 客户端 |
| F4 | 图编排 | write-agent 自身无检索工具，"先检索再写"只靠 prompt → pack 空时凭记忆编造 | supervisor 执行 call_write_agent 前校验本轮检索∪会话证据非空，为空返回结构化占位 |
| F5 | 图编排 | 熔断失败判定漏 registry 的 `[ERR_FILE_NOT_FOUND]/[ERR_PERMISSION]/[ERR_PARSE]` 标签 → 文件/权限类连续失败永不熔断 | 统一读 `artifact._meta.status`（registry 已产出），文本只作兜底 |
| SQL-1 | 会话 | SQLite 连接策略矛盾：同库 6 模块，WAL/busy_timeout 不一致；锁超时被宽 except 吞 → save_messages/set_checkpoint **静默失败（消息丢失）** | 统一 `_connect()` 工厂（WAL + busy_timeout=30s）；save 路径失败必须 log error + 至少重试 1 次 |
| SQL-2 | 会话 | 同会话并发无串行化：checkpoint 读写竞态，摘要互相覆盖 | per-session 写锁（或 SQLite BEGIN IMMEDIATE） |
| MEM-1 | 会话 | estimate_tokens 只计消息内容（漏 system/tool schema/status 块），无"估算 vs 实际 usage"对比回路 → 1M 窗口压缩阈值水位不可知 | 每次 LLM 调用后记 `diag` 估算 vs 实际输入 token 比值，≥10 个样本后校准系数 |
| OBS-1 | 基建 | progress_bus `_usage_last` 全局不重置：新会话/缓存命中致 total 回落时 delta=0 静默丢失，并发 key 互踩 | 改 per-request 字典（contextvar），请求结束清理 |
| OBS-2 | 基建 | `clear_tool_timers()` 每请求清全局计时表 → 并发请求互抹心跳 | 工具计时表改 contextvar（per-request） |
| OBS-3 | 基建 | 断连保活后 SSE 队列无人消费，graph 继续 emit → 无界增长内存 | emit 前检查消费端存活标记；队列满即丢弃并计数 |

### 3.3 检索口径红线（🔒 待数据集验证）

| # | 问题 | 证据 | 为什么现在不能动 |
|---|---|---|---|
| BM25-1 | BM25+ "plus" 项实现为 `tf+δ·tf` 而非规范 `tf·(k1+1)+δ`，与规范实现相对差 ~43%、top-10 仅 5/10 重合 | 实机数值验证 | 检索排序口径改变 = 问答质量风险；无离线 Top@k/nDCG 基线（119 测试多为字符串断言，保不住检索质量）→ 等 gold_dois |
| E5-1 | e5 指令前缀三处分裂：语料/主路径查询无前缀、HyDE 查询有 `query:` 前缀、LTM 单侧有前缀 → 同 query 不同路径不同向量排序 | fastembed 0.8 不会自动加前缀（已查包源码） | 同上；改前缀需全量重嵌入或双索引过渡 + 端到端回归 |
| RER-1 | reranker CPU 模式 per-thread 会话重载整份 2GB ONNX（默认线程池 ~32 线程时多份驻留 + 每线程首次加载数秒） | 代码路径确认 | 修复=共享单会话+锁，需在真实并发下验证吞吐/内存，属"优化验证"类 |

---

## 四、P2/P3 清单（按簇，供路线图排期）

### 基础设施/API（E 簇）
| # | 级 | 问题 |
|---|---|---|
| E-2 | P2 | 观测五套机制职责纠缠：business/feedback logger 全文复制（99% 同体）、retrieval.log 无 req 字段、light 模式 retrieval_done 双发 |
| E-3 | P2 | SSELogHandler 零挂载 → bridge_logs 死管道 |
| E-4 | P2 | 版本单源名存实亡：VERSION=8.9.0 vs 代码注释 v8.10~v8.13 |
| E-5 | P2 | context_usage 中 cache 绝对值混入 delta 语义 |
| E-6 | P2 | jobs.py 每调用新建 SQLite 连接 + 重复 DDL |
| E-7 | P2 | snapshot 默认配置下 dynamic_* 全渲染为空 |
| E-8~11 | P3 | Fast Guard 死代码 / setup_logging 破坏性重建 / done 前固定 sleep(0.5) / switch_model 死代码 |

### 会话/记忆/上下文（C 簇）
| # | 级 | 问题 |
|---|---|---|
| C-2 | P2 | PII 脱敏只挂文件 handler（控制台与 retrieval.log 不脱敏） |
| C-3 | P2 | LTM 域过滤 OR 条件致跨会话高置信事实泄漏 |
| C-4 | P2 | `srv:` 30s 幂等桶吞掉自然重复提问 |
| C-5 | P1→P2 | load 热路径 O(全历史+500×embed)；压缩在 DOI/JSON 中段截断；target_ratio/summarize_ratio 死配置（"一次压到 50%"从未兑现） |
| C-6 | P2 | LTM ADD-only 无 TTL/容量上限，limit 500 恒取最新（旧事实永远占位）；resident_cards fact_key 主键跨域互踩 |
| C-7 | P2 | `_hard_trim` 按字符截断会切在工具调用配对消息中间 → API 400；摘要以 HumanMessage 注入被 `_split_turns` 当作用户轮次（多轮语义污染） |
| C-8 | P3 | 死代码/无效变量：replace_history 无调用、history_summary 恒 None、compression_len 恒 0、双规则摘要重复实现；`_session_locks`/`_compaction_failures` 无界增长；memory_store 表 DDL 三处分发 |

### 工具/执行器（D 簇）
| # | 级 | 问题 |
|---|---|---|
| D-2 | P2 | 模型选择三源并存 + llm_pool 覆盖不全：switch_model 后写流水线/HyDE/classify 仍用旧模型 |
| D-3 | P2 | 工具执行四通道并行：registry 通道有沙箱/超时/offload，图内联与流水线直调绕过 `_check_tool_sandbox`（ask 审批模式形同虚设） |
| D-4 | P2 | 检索口径双实现：search_multi 与 search_hyde 各写一遍 RRF/阈值/日志，已漂移过一次（v8.10r 注释自证） |
| D-5 | P2 | HyDE 阈值失败 → 整条检索管道重跑（最坏 2× 开销，60s 工具超时下易炸） |
| D-6 | P2 | async 工具 read_local_file 内全同步 IO → 50MB PDF 冻结事件循环（对比 pdf_read 已用 to_thread） |
| D-7~11 | P3 | 中文 query Jaccard 去重失明（整段中文=1 token）/ 柑橘词表 4 处不一致 / 检索预算计数只统计末轮 / 三份收尾 prompt+两份兜底+两份截断 / for-else 死分支+工具绑定四源 |

### 图编排（B 簇）
| # | 级 | 问题 |
|---|---|---|
| B-6 | P2 | light 模式工具集不对称且静默降级：写综述类意图产出纯聊天零落盘，无提示 |
| B-7 | P2 | output_path 缺失兜底只打在 plan 路径，react_fallback 无路径时静默不落盘 |
| B-8 | P2 | 默认 llm_factory 每次新建 ChatOpenAI（未走 llm_pool） |
| B-14 | P2 | session_evidence 写入无幂等保护（save_messages 有 idempotency_key，save_evidence 没有）→ 同轮重试重复记账、账本膨胀 |
| B-9~13 | P3 | light supervisor seen 集每轮重建 / 直写快捷路径同步 write / done 事件两分支 schema 漂移+死节点名+固定 0.5s / strategy_cards 死代码 / agent_map 与 skill 截断逻辑各实现两遍 |

### 写作链（F 簇）
| # | 级 | 问题 |
|---|---|---|
| F-4 | P2 | "保存短内容"路由边界：文件已存在+保存类 goal 恒进 modify 报"未找到目标章节"；<2000 字符直写不落盘 |
| F-5 | P2 | _unify_references 状态机边界：正文行恰为"参考文献"误切引用区；无标记章节条目全进 body；`## 摘要` 子标题误切新章 |
| F-6 | P2 | 单位混用：max_per_section 按 token 推导却与字符直接比较（4000→3000 巧合一致，调参即脱钩） |
| F-7 | P2 | 单章调用不传 max_tokens（输出上限交给 API 缺省，只能事后发现"超长"发现不了"截短"）；压缩重试无封顶 |
| F-8 | P2 | 技能注入"双轨三结果"：plan 成功走 skill_map、失败走截断串、react 走 extra——渐进式披露只在成功路径生效 |
| F-9 | P2 | 同一事实五信道重复上报（SSE/jobs/blog/diag/logger），无一致性保障 |
| F-10~14 | P3 | 魔数与重复公式（max_per_section×3 处）/ running_context 死代码（并行模式恒传 ""，docstring 仍宣称衔接）/ _read_prompt 绕过 lru_cache / diag 事件缺 dur_ms（本轮已补 span 用法待推广）/ modify 路径吞写错误 |
| F-15 | P3 | 断点续传侧死代码与静默降级：find_resumable_task/cleanup_stale_tasks 生产无调用者；任务状态 `json.dumps(...)[:2_000_000]` 字符硬截断可中段截断 → 恢复时 json.loads 静默失败；resumed 材料 `len(saved)>=len(fresh)` 即覆盖本次新检索（可能丢弃更优证据） |
| F-16 | P3 | 并行（sem=3）落地时"前章摘要衔接"被放弃但 running_context 死代码与文档未清理——章节独立性假设对讨论/展望/结论类强关联章节不成立，应把串行衔接做成质量选项（删 or 串行，二选一） |

### 检索/引擎（A 簇）
| # | 级 | 问题 |
|---|---|---|
| A-4 | P2 | 候选漏斗 240→20→10：rerank 只覆盖 RRF 前 20，排 11~20 的"可过阈值"候选不可见；`passed x/10` 日志给人高比例错觉 |
| A-5 | P2 | 后端度量无校验：`score=1-_distance` 硬编码假设 cosine，索引失败静默走 L2 flat 兜底 → 负分/量纲错乱混入 RRF |
| A-6 | P2 | last_empty_reason/runtime_failed_batches 共享可变状态无锁，并发会话归因互覆盖；close() 未清空残留 |
| A-7 | P2 | HyDE 失败全量重跑 + _hyde_cache 无锁（并发重复 15s LLM 调用） |
| A-9 | P2 | Qdrant 重试块/CPU 兜底体/双检索路径阈值日志段三处逐字复制 |
| A-10 | P2 | 主路径 embed_docs 绕过 query LRU（最贵的计算走了最慢的缓存路径）；_chunk_full 为 20 候选提前取全文，10 条被 rerank 丢弃 |
| A-11 | P2 | GPU 锁策略不一致：embedder 仅 DML 锁、reranker DML+CUDA 都锁 |
| A-12 | P2 | reranker/embedder 任何运行时异常（含输入类）→ _force_cpu 永久粘滞降级，进程生命周期不恢复 |
| A-13~14 | P3 | idx_map 抽样 200 点覆盖率 0.17% + 逐点 warning 刷屏 / 死计时变量（本轮已接 diag） |

---

## 五、架构级观察（合并 6 簇）

1. **三层 ReAct 循环近似复制**（expert supervisor / light supervisor / run_agent）：轮次循环、重试、预算、收尾、截断、去重各实现一遍——"修一处漏两处"类缺陷（D-3/D-7/B-9/F-5）的共同温床。**收敛为公共 `AgentLoop` 基座是单点收益最大的重构**。
2. **工具执行缺单一出口**：registry 的 PartitionedToolNode 已是"半套"执行层（沙箱/超时/offload 都在），但图内联与流水线直调绕过它 → 同一工具不同入口安全语义不同（D-3 是直接后果）。
3. **检索口径散落实现细节而非策略对象**：RRF 权重、动态阈值、去重、预算全以"实现+注释"存在，双管道（search_multi/search_hyde）各读一遍 settings——已实际漂移过一次。
4. **证据生命周期为"多份拷贝"而非"单一事实源"**：chunk 全文同时存在于 ToolMessage/artifact/回执/证据账本/历史五处，截断阈值 1000~40000 各不相同 → token ×3 浪费 + 引用编号跨层难一致。**证据单例 + render_evidence() 引用**是正确收敛方向。
5. **安全边界自实现且修复节奏不同步**：同款"前缀/相对路径逃逸"在 readfile 修过（v8.4.14）而 file_ops/pdf_read 仍是旧式判定（本轮已统一）；建议集中式 `PathPolicy`（resolve + is_relative_to + 根列表）供所有文件类工具共用。
6. **检索全局状态四层持有**：MultiBatchRetriever 双检锁单例 + search.py 再包装 + eager_load_rag + light_graph 直接构造——同一对象 4 处拉取、生命周期各自管理；700 行 god 类同时承担加载/缓存/检索/RRF/阈值/日志/降级记账。
7. **治理哲学"事后标记"而非"事前拦截"**：检索侧有代码级 precondition（DEDUP/预算拦截），写作侧全靠 prompt 约定（"先检索再写"写在 tool description，引用支撑只打标不拦截）——两条业务线治理成熟度不对等（F4）。
8. **观测体系应收敛为两层**：diag 业务事件层（JSONL，req 为第一连接键）+ agent.log 技术层；SSE/jobs/blog 由事件派生。business/feedback 两 logger 先合并为同一实现（99% 复制）。
9. **超时取消语义有坑**：`asyncio.wait_for` 只取消 await——`run_in_executor` 提交的线程仍在后台完成推理（占 GPU 锁/CPU），超时重试会与后台残留**叠加并行负载**；registry 默认线程池 + 60s 工具超时下最坏并发数远超可见值。
10. **缓存机制碎片化**：query embedding LRU（仅 embed_query 路径）/ HyDE dict（无锁）/ 文本 LRU / BM25 磁盘缓存四处独立实现；主路径 search_multi 恰好绕过 query LRU——最贵的计算走了最慢的缓存路径。

---

## 六、诊断缺口（6 簇建议汇总，标注覆盖状态）

| 指标 | 状态 |
|---|---|
| 请求级 done 带 usage 原始绝对值（当前只有 delta 语义） | 📋 待补 |
| retrieval.log 带 req 字段（当前检索文本日志无关联键） | 📋 待补 |
| SSE 丢弃/积压计数（孤儿队列监控） | 📋 待补 |
| 工具计时泄漏清单（per-request 化后） | 📋 待补 |
| SQLite 连接/busy 统计 | 📋 待补 |
| `plan_llm`/`section_llm` 含等待时间（并行 sem 排队是隐蔽延迟源） | 📋 待补（diag_span 包 _call_llm 一处覆盖 7 调用点） |
| 检索 stage_ms / pool_sizes / rerank 分数直方图 / empty_reason 分布 / idx_map 聚合计数 | ✅ 部分（stage_ms 已接；其余待补） |
| write 阶段级 pipeline_phases（plan/execute/unify/prune/verify/publish 各自 ms） | 📋 待补（diag_span 包各阶段） |
| HyDE 成功率/耗时/cache 命中 | 📋 待补 |
| governance_counters（dedup_blocked / search_budget_blocked / budget_skip / 熔断触发峰值）请求级聚合 | 📋 待补（当前治理动作只打 logger，无业务级聚合，预算/熔断过强过弱无从判断） |
| write_heartbeat（elapsed_s / section_idx / 排队等待 ms / 队列深度）+ usage source 细分（plan/section/condense，当前统一 write_pipeline 无法区分阶段成本） | 📋 待补（断连后判断任务卡在生成/等待/写盘；443s 中并行排队是隐蔽延迟源） |

---

## 七、修复路线图（建议顺序）

1. **第一批（已修 ✅，v8.13）**：A2/F1/A-01/A-10/A-11/A-07/A1(发布门禁)/F8/API-1/API-2。
2. **第二批（已修 ✅，v8.13-b2）**：F3/A3 续传状态机（resumed 陈旧重领 + partial 可领 + aborted 兜底 + 全量续传发布）→ SQL-1 连接统一（manager/memory `_connect_db`：WAL + busy_timeout=30s，切换 2s 快失败防阻塞）+ set_checkpoint 失败升级 error → SQL-2 补锁（clear/title 写路径 per-session 锁；memory `_mem_lock` 改 RLock 防自死锁）→ F5 熔断识别全部 `[ERR_*]`（排除 HITL_REJECT）→ A1 配套（unify/prune 悬空引用删除标记，dropped 记录"已删除"）。新增测试 3 项（resume_state_v813 / full_resume_publishes / unify_dangling_ref_removed），status 语义改发布结果驱动。
3. **第三批（观测收敛）**：business/feedback logger 合并 → context_usage 语义修正 → 版本单源 → SSELogHandler 挂载或删除。
4. **第四批（结构性重构）**：
   - 第一步（已修 ✅，v8.13-b4a）：**PathPolicy 集中**（新增 `core/path_policy.py`：resolve+is_relative_to+根列表，统一 readfile/file_ops/pdf_read/registry 的路径判定，锚定 PROJECT_ROOT 消除 CWD 相对——含 OFFLOAD_DIR 锚定修复）+ **工具执行单一出口**（registry 新增 `run_tool_checked`：沙箱+超时+offload+错误分类；write_pipeline 3 处直调、expert_graph 3 处内联全部收敛，ask 审批对写作真正生效；顺手修 A-06 read_local_file async→sync 阻塞、A-15 modify 吞错）。
   - 第二步（待做）：检索双管道收敛单管道 + 证据单例 render_evidence（各配一次真实问答 A/B 验证）。
   - 第三步（待做）：AgentLoop 基座。
5. **数据集到位后**：BM25+ 公式修正 + e5 前缀统一（先建离线评测基线 E1 脚本再动）。
6. **写作链瘦身**：references 三函数拆 `core/references.py`（纯函数状态机，最易测试、性价比最高）→ max_per_section 单函数 → 魔数常量块 → running_context 决策（删 or 串行质量选项）。

---

## 八、与《后续计划_优化与论文实验.md》的衔接

- 本文「第四批」结构性重构与该文档「A 类检索改进（Contextual Retrieval）」方向一致，可合并排期；
- 本文「数据集到位后」红线项与 E1 检索质量实验互为前置：**先写 eval_retrieval.py + 收集 gold_dois，再动 BM25/前缀**；
- diag 层直接服务 E4 成本实验（token/耗时/cache 自动落 JSONL，无需新埋点）。
