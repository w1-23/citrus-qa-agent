# 未来建议待办项目（v8.4.6 整理，2026-08-14）

> 依据：《深入理解AI Agent》（李博杰 v1.4）+ 两次全架构审核 + 运行观察。
> 状态标记：【已做】【已采纳·待验收】【保留·分支优化】【待定】【待做】。
> 评估类项目按用户决定暂缓，待"输出画像"运行数据积累后再启动。

## 一、必做优化（功能正确性/合规，建议下一批）

| # | 项目 | 依据 | 方案（一种） | 状态 |
|---|---|---|---|---|
| B1 | light 图无预算前移检查（expert 每次调用前查 hard 阈值，light 没有；light ≤2 轮风险低） | 书 §2.7.4 分层压缩 / INV-08 | light_supervisor 调用前同样 estimate_tokens + hard 阈值收尾 | 待做 |
| B2 | 压缩硬截断丢标识符：`context_budget._hard_trim` 丢弃中间轮次时未保留 DOI/evidence_id（书 2.7.5"保留标识符"） | §2.7.4/2.7.5 | 丢弃前先提取被丢轮次的标识符行并入摘要/视图尾部 | 待做 |
| B3 | 证据账本 `report_text[:8000]` 无截断标记（v8.4.6 全文回执 2~3 万字符后更明显） | INV-08 截断透明 | 截断时追加"[报告已截断，全文见 messages 表]" | 待做 |
| B4 | light 预检索与 light supervisor 补充检索之间无去重共享（expert 已有 seen_queries） | v8.4.6 F4 同源 | light supervisor 调 citrus_rag_search 前查 seen_queries（预检索 query 入集） | 待做 |
| B5 | 提示注入面：chunk 全文进上下文后（v8.4.5/8.4.6），证据块的指令边界只有 `<context>` 一层声明 | §2.4.7 | 证据块 `<evidence data_only>` 标签 + 注入声明 + `> ` 前缀消毒 | 待做 |
| B6 | 日志无脱敏（business.log/agent.log/sessions.db 原文存 query/answer/chunk）——多用户分发前硬门槛 | §3.1.8 + 实验3-3 | blog/logger 输出前正则脱敏（邮箱/手机/身份证/密钥），进阶本地小模型 | 待做 |
| B7 | 检索不感知对话语境（多轮追问不带上文约束） | §3.3.5 上下文感知检索 | 最近 1-2 轮 query 自动附入 retrieve-agent 上下文（previous_queries 块） | 待做 |
| B8 | 验证靠自由文本（熔断判定解析 `rtext.startswith("[Error")` 等） | §1.2.2"验证只看结构化数据" | 工具结果 envelope `{status, code, payload}`，熔断/统计只读字段 | 待做 |
| B9 | 写作"自检状态"是模型口头声明，无代码校验（写后 ref 校验已有，但回执中的"所有数值均有文献来源"无验证） | §1.2.2 验证 / 实验7-17 教训"口头声称去验证" | 发布前代码校验（引用配对+必备章节+数值↔证据片段抽查）并写入回执 | 待做 |

## 二、可做优化（体验/成本/演进，视需要排期）

| # | 项目 | 依据 | 方案（一种） | 状态 |
|---|---|---|---|---|
| O1 | evidence 全文落上下文 vs 分支优化（evidence_id 文件/折叠式注入） | 用户决定"先观察效果" | 现行为全文进上下文；观察 token 成本与追问体验后，再决定是否加 feature flag 分支 | 保留·分支优化 |
| O2 | 评估体系（黄金问答集 + LLM-as-a-Judge + 失败归因 + 轨迹前缀回归） | §6 全章 | 20~30 条标注问答 + tests/eval_qa.py（FAST rubric 打分、定位首个错误步骤） | 待定（评估类暂缓） |
| O3 | proposer-reviewer 校验闭环（回答生成后 rubric 审核、不达标重生成 1 次） | 引言 / §10.5.3 / 实验6-6 | FAST reviewer 三维 rubric（事实支撑/引用一致/缺口声明）+ 覆盖率指标（v8.4.6 已埋 evidence_avail/cited 度量） | 待定（评估类暂缓） |
| O4 | 特性开关做实（feature_flags 框架 0 个开关） | §6.10.3 | 给 reviewer/上下文检索/并发写等各加运行时开关，可一键回滚 | 待做 |
| O5 | 提示词版本化快照 | §6.10.4 | `python -m src.prompts.snapshot` 确定性渲染入库 + diff + 变更跑评估 | 待做 |
| O6 | Skill 渐进式披露（两段式：名称+触发条件 → 按需全文） | §2.5 / §4.8 | 写任务第一段 ≤500 字摘要，模型标记 use_skill(id) 后再补全文 | 待做 |
| O7 | 经验沉淀与睡眠学习（👍/👎 落库 → 聚类生成经验草案 → 人工确认进 skills） | §8.2.1 / §8.3.5 | run_feedback 表 + 离线聚类脚本 + 经验草稿 Markdown | 待做 |
| O8 | BM25 索引持久化（每次启动重建 ~10s） | §3.2 离线建索引 | 语料 hash + pickle 缓存，命中直接加载 | 待做 |
| O9 | LTM 定期整理（ADD-only 前提下同义聚类合并为新版本行） | §8.3.5 | 启动/日终后台任务 embedding 相似度 >0.95 聚类 | 待做 |
| O10 | 前端显式"停止"按钮（目前只能刷新页面打断） | §4.7 事件驱动异步 | 复用 AbortController，发送按钮旁加停止键 | 待做 |
| O11 | 子代理独立 token 预算告警（后台 spawn 任务日计数，超限跳过+告警） | §10.5.3 循环失控 | 每日计数器 + 日志告警；API key 物理分离留到多租户 | 待做 |
| O12 | 多模态图表感知（pdf 图表信息丢失） | §9 多模态（调研项） | 模型支持视觉后，图表页图片+文本描述入检索 | 待定 |

## 三、本轮已做（v8.4.6，待提交）

- 【已做】F1 light 代码级预检索（retrieval_context 注入 + artifacts 合流）
- 【已做】F2 历史证据引用进侧栏（historical H1..Hn）
- 【已做】F3 执行日志层级化
- 【已做】F4 检索代码级去重（Jaccard≥0.85）+ 确定性证据回执（chunk 全文进上下文）
- 【已做】F6 格式模板去刻板化（证据量驱动）+ 决策指南硬性覆盖要求
- 【已做】F7 写作草稿-发布 + 写任务材料并入历史证据（plan_execute 恢复）
- 【已采纳·待验收】并行写章（生成并发 3、按序写盘；质量影响论证见提交说明）
- 【已做】输出画像（_build_output_profile：证据量 → 篇幅/覆盖下限注入状态栏尾部；business.log 埋 evidence_avail/cited 指标）
- 【已做】marked 锁版 4.3.0（[object Object] 根治）、版本 8.4.6 单源

## 四、上下文管理审计结论（对照书第 2 章，2026-08-14）

**达标项**：静态前缀（§2.2.5/2.3）、动态内容一律追加尾部（§2.3 铁律）、`<agent_status>` 不进历史（§2.6.3）、压缩只在用户轮边界（§2.7.4）、checkpoint 增量视图+防摘要套摘要、append-only 存储（INV-10）、噪声修剪保持 INV-01 配对（§2.7）、子代理上下文隔离（§2.7.7）、prompt_cache 命中观测（§2.3.3 双层缓存）。

**偏差项（对应上表 B1~B4 及备注）**：
- B1 light 无预算前移；B2 硬截断丢标识符；B3 report_text 截断无标记；B4 light 去重缺口；
- 观察项①：1M 窗口下 soft 阈值 75%（=750K tokens）几乎不触发压缩，checkpoint 机制长期休眠——窗口充足故非缺陷，但压缩路径缺少实战锻炼，建议评估集建立后做一次 80% 占用演练（书 §2.7.4 L3 熔断路径同理）；
- 观察项②：`build_human_message` 的 `<conversation_summary>` 分支为死代码（摘要已嵌入视图消息），无害但建议清理；
- 观察项③：v8.4.6 全文回执使单轮检索在历史中新增约 2~3 万字符（≈8~10k tokens），1M 窗口可容纳；已通过 prompt_cache 命中（历史实测 cache_hit 768→65536→68864 tokens）抵消大部分重复成本——**这正是"观察效果"阶段要重点看的指标**。
