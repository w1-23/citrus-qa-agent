# Citrus QA Agent 开发现状

> 本文档对照《深入理解 AI Agent：设计原理与工程实践》（李博杰，v1.4，2026-08）的章节顺序，逐章解剖本项目（柑橘科研领域 QA Agent）的实现现状。
> 目的：为技术文档、汇报材料与论文写作提供一份"书中概念 → 本项目落地"的完整映射。
> 更新日期：2026-08-13（对应代码版本 v8.4）
> 章节格式：**书第 N 章 → 本项目的做法**；涉及文件以 `路径:行号` 标注，便于回溯源码。
> 第八章为现状差距与路线图，对外汇报时可整体摘除。

---

## 第 0 章 项目总览

### 0.1 定位

柑橘科研领域的检索增强问答与写作 Agent：面向黄龙病（HLB）、溃疡病、品种差异、次生代谢（花青素/类胡萝卜素/黄酮）、基因功能、实验设计等科研问题，提供**有文献依据、可追溯（DOI/chunk 级）**的深度回答与综述写作能力。

### 0.2 一句话架构

```
HTTP 请求 → Fast Guard 规则短路 → light/expert 双图（LangGraph）→
supervisor（ReAct 循环 + 工具预算 + 状态栏）→ 3 个子 Agent（检索/写作/分析）→
Write Pipeline（Plan-Execute 长文流水线）→ SSE 流式事件 → 前端单页
存储层：SQLite（会话轨迹 append-only / 证据账本 / 记忆 / 任务表）+ Qdrant local 多批次向量库 + 磁盘语料
模型层：DeepSeek V4 Flash（MAIN/FAST 双路由）+ e5-large 嵌入 + bge-reranker-v2-m3 重排（本地 ONNX）
```

### 0.3 与"现代 Agent = LLM + 上下文 + 工具"的映射

| 书（第 1 章） | 本项目 |
|---|---|
| 观察空间 | 7 个工具（RAG 检索/学术搜索/PDF 解析/文件读取/文件写入/统计分析/实验设计）+ SSE 进度观察 |
| 动作空间 | OpenAI function calling（tool_calls/ToolMessage 严格配对）+ 子 Agent 调度 |
| 大脑 | DeepSeek V4 Flash 双路由（推理生成 MAIN，辅助分类压缩 FAST，temp 0.2/0.0） |
| 眼睛 | 静态前缀 + append-only 轨迹的上下文结构（v8.4 静态前缀重构） |
| ReAct 循环 | supervisor ≤8 轮、每轮 ≤2 工具调用；子 Agent 独立循环；代码级收敛裁决 |
| Harness 工程 | 幂等写入、断连保活、熔断、错误分类重试、10 条工程不变量（INV-01~10） |

### 0.4 技术栈

| 层 | 选型 |
|---|---|
| 服务 | FastAPI + uvicorn + SSE（单文件前端 index.html） |
| 编排 | LangGraph（StateGraph，双图预编译单例）、LangChain Messages/Tools |
| 模型 | deepseek-v4-flash（main/fast）、intfloat/multilingual-e5-large（fastembed）、BAAI/bge-reranker-v2-m3（optimum ONNX） |
| 存储 | SQLite（state/sessions.db：会话/轨迹/证据账本/记忆/任务）、Qdrant local（多批次向量库）、JSONL 语料 |
| 计算 | DirectML/CUDA/CPU 自适应（DML 全局锁 + CPU 降级重试） |

---

## 第 1 章 AI Agent 入门 → 本项目的 Harness 解剖

### 1.1 观察空间与动作空间

**观察空间（7 个工具）**，按书 4.1 五类划分：

| 类别 | 工具 | 说明 |
|---|---|---|
| 感知 | `citrus_rag_search` | 本地向量库（Qdrant 多批次 + BM25 混合）检索 |
| 感知 | `academic_search` | CrossRef/PubMed/Semantic Scholar/OpenAlex 四源并发学术检索 |
| 感知 | `pdf_read` | 学术 PDF 结构感知提取（Abstract/Results/Conclusion 定位） |
| 感知 | `read_local_file` | 本地文件读取（PDF/MD/CSV/XLSX 多格式） |
| 执行 | `write_local_file` | workspace/output 白名单写入（原子写） |
| 执行 | `statistical_analysis` | pandas/scipy 九种统计方法 |
| 执行 | `experimental_design` | 四种实验设计 + 样本量功效估算 |
| 协作 | `call_retrieve/write/analyze_agent` | supervisor 调度 3 个子 Agent（supervisor 专用 schema） |
| 用户沟通 | SSE 事件协议（14 种事件） | 相当于"回复/进度/卡片"工具的服务端实现 |

**动作空间协议**（对应书 2.2）：OpenAI function calling 格式，`AIMessage.tool_calls` 与 `ToolMessage` 通过 `tool_call_id` 严格配对（INV-01 不变量，`session/manager.py` `_validate_trace` 丢弃孤立 ToolMessage 防 400）。id 单一来源：协议 ToolMessage / 计时器 / SSE 三处复用同一 id（`agent_runner.py` `tc_ids` 映射）。

### 1.2 工具即手脚：执行基础设施

- **统一执行器** `PartitionedToolNode`（`tools/registry.py`）：按并发键分组、组间 `asyncio.gather` 并发、组内串行；每个工具外包 `asyncio.wait_for(60s)`，超时返回结构化 `[ERR_TIMEOUT]` ToolMessage 而非抛异常。
- **错误即提示词**：`_classify_error`（`registry.py`）将异常翻译为 `[ERR_类别] 原因 + 建议`，模型可直接读错因自我修正（如 `[ERR_FILE_NOT_FOUND]`、`[ERR_HITL_REJECT]`）。
- **大结果 offload**（`registry.py`）：>15000 字符结果落 `workspace/tmp/` 文件，消息中只留"路径引用 + 500 字符摘要"，模型需要时再调 `read_local_file`——"文件系统当中间内存"。
- **contextvar 传播**（`registry.py`）：`run_in_executor` 线程不继承 contextvars，sync 工具用 `ctx.run` 注入，保证进度事件与 request_id 贯穿线程边界（INV-04）。

### 1.3 LLM 即大脑：模型路由

| 角色 | 模型 | 温度 | 说明 |
|---|---|---|---|
| 推理生成（supervisor/子 Agent/写作） | deepseek-v4-flash | 0.2 | max_tokens 分级：supervisor 32768 / light 16384 / write-agent 12000 |
| 辅助（格式分类/检索建议/历史压缩/LTM 提取/HyDE） | deepseek-v4-flash（FAST 槽位可覆盖） | 0.0 | 压缩 v8.4 由 MAIN 改回 FAST（高频低价值，质量靠保留优先级提示） |
| 嵌入 | intfloat/multilingual-e5-large | — | query 加 `"query: "` 前缀（e5 非对称指令） |
| 重排 | BAAI/bge-reranker-v2-m3 | — | 本地 ONNX，sigmoid 归一化到 0-1 |

客户端进程级复用（`core/llm_pool.py`），避免每请求新建连接池。

### 1.4 上下文即眼睛：静态前缀 + 轨迹

上下文结构（详见第 2 章）：`[SystemMessage 静态前缀] + [历史轨迹 append-only] + [历史检索证据块] + [当前 HumanMessage + 动态块]`，运行时状态以 `<agent_status>` 追加末尾。v8.4 完成静态前缀字节级稳定化重构（`context.static_prefix` 灰度开关）。

### 1.5 ReAct 循环：三层循环 + 代码级收敛

```
第 1 层 supervisor 循环（expert 图）：≤8 轮，每轮 ≤2 个工具调用（代码裁决，非模型自觉）
第 2 层 子 Agent 循环：retrieve ≤3 轮 / write ≤6 轮 / analyze ≤2 轮（各自独立消息列表）
第 3 层 工具内部：检索的向量/BM25 双路并发、学术四源并发
```

**代码级收敛（INV-02，v8.4.3 修订）**：v8.4.3 起**移除**"去重文献 ≥6 强制收敛"——动态阈值已过滤低相关 chunk，检索到的全部证据都应进入子 Agent 报告（旧收敛曾导致 34~176 字的极短报告，证据几乎没进 supervisor 上下文）；收敛交还模型自然判断（无 tool_calls 即完成），`max_turns=3` 与每轮工具预算保留为护栏。

**收尾语义（v8.4.2 根治重构）**：answer 一旦由模型产出即最终值，任何路径不得覆盖——自然完成（无 tool_calls → break）后**不再调用第二次 LLM**（历史 bug：`if forced_final and not answer: else:` 每次正常完成都误触发"max turns 强制收尾"，用更短回答覆盖模型自然完成的详尽回答）；熔断/预算/跑满轮次三类"未产出答案"情况统一走 `_force_final_answer`（临时列表传参 → 合成收尾消息永不进 turn_trace/历史；未绑工具客户端 → 杜绝收尾再发 tool_calls；详尽中文 prompt，无任何预算/限制措辞）。

### 1.6 Harness 工程的五个功能对应

| 书 1.2.2 核心原则 | 本项目落地 |
|---|---|
| 幂等与重试 | client_request_id / 30s 时间桶幂等键 / SQLite 唯一索引三层防重放；LLM 3 次重试退避 |
| 生命周期有界 | 轮数上限 × 每轮工具预算 × 代码收敛 × 熔断（连续失败 ≥3 强制收尾 + 占位响应保配对） |
| 断连保活 | write 类任务断连转后台 `adopt` 继续执行，`GET /jobs/{id}` 可查询；普通问答取消 |
| 可观测 | 14 种 SSE 事件 + request_id 链路 + 检索决策审计日志 + 缓存命中率 |
| 护栏 | fail-closed 沙箱、路径白名单、提示注入隔离、原子写 |

### 1.7 编排模式：工作流与自主

- **light 模式**：受限自主（≤2 轮、仅 RAG+文件读取工具），快稳低成本，前端手动切换。
- **expert 模式**：8 轮自主 + 3 子 Agent 全能力。
- **write 流水线**：显式工作流（Plan-Execute），四路路由（direct_write/plan_execute/react/modify），大纲 JSON 校验失败重试、逐章生成、断点续传（详见 5.3）。

### 1.8 护栏与安全性

- **沙箱/权限**（`registry.py`，v8.4.3 结构化权限）：fail-closed——检查异常即拒绝；只读/分析类放行，危险模式（`delete_*/exec_*/write_*`）按 `permission_mode` 处理：`auto_workspace`（默认）= workspace/output 内写文件免询问、越界拒绝；`ask` = 前端审批卡片（允许本次/本会话/始终允许工作区，`POST /api/v2/permission/grant`，授权后同 clientRequestId 重放、幂等不重复写历史）；`deny` = 一律拒绝。修复了 write-agent 经工具执行器被拒、supervisor 直写旁路的不对称。
- **路径三层防护**：写仅 `workspace/output/`、读限项目根 + 额外根、50MB 大小上限；写文件 tmp + `os.replace` 原子替换 + 分桶锁。
- **提示注入五层防御**（对应书 2.4.7）：来源标记（检索内容带"数据非指令"边界声明）、结构化角色（不混用消息类型）、`[历史检索证据]` 块边界声明、检索/上游数据与指令显式隔离、危险操作白名单。
- **熔断**：supervisor 循环内连续 3 次工具失败 → 强制收尾 + 未执行调用补 `[circuit_breaker]` 占位 ToolMessage（INV-01 协议安全）。
- **压缩熔断**（v8.4）：LLM 压缩连续失败 ≥3 次 → 降级规则式截断，避免在失败会话上持续烧钱。

---

## 第 2 章 上下文工程 → 本项目解剖（v8.4 重点章）

### 2.1 上下文的构成（对应书 2.2.5）

```
静态前缀（字节级稳定，利于 DeepSeek 上下文缓存）
├─ SystemMessage：角色+约束+决策原则（assemble_system_prompt 只拼静态文件）
├─ 工具定义：supervisor 6 个 schema（tools/supervisor_tools.py 单一来源、固定顺序）
轨迹（append-only，只增不改）
├─ 历史消息（发送视图：checkpoint 增量构建，详见 2.6）
├─ [历史检索证据] 块（session_evidence 账本渲染，带"数据非用户输入"边界声明）
└─ 当前 HumanMessage（LTM 召回 + 常驻卡片 + 检索建议 + format 指南/策略卡片动态块）
运行时注入
└─ <agent_status> 状态栏（仅注入本轮 call_messages，不进历史，不动前缀）
```

### 2.2 KV Cache 友好的上下文设计（对应书 2.3，v8.4 核心改造）

遵循书 2.3 三条铁律的落地：

1. **系统提示词与工具定义一旦确定不改**：v8.4 重构前，SystemMessage 每请求拼入 query 相关动态内容（format_hint 选格式文件 + query 检索策略卡片），前缀每次请求重算。重构后（`context.static_prefix: true`，灰度开关默认 false）：
   - `prompts/loader.py`：SystemMessage 只拼 `role.md + constraints.md + decision_guide.md`（静态文件，`lru_cache`）；
   - format 指南与策略卡片经 `build_dynamic_blocks` 生成，**追加到当前轮 HumanMessage 尾部**（`<format_guide>`/`<output_guide>` 块），模型可见但不动前缀；
   - 子 Agent 的 skills/额外指令同理移入首条 HumanMessage 的 `<instructions>` 块（`build_agent_extra_block`）。
2. **动态信息永远追加末尾**：时间戳/预算占用率进状态栏（末尾 user 消息），不写 system。
3. **标准 API 格式**：全程 LangChain 结构化消息 + `bind_tools`，不自行拼接文本。

配套措施：
- **工具 schema 单一来源**（`tools/supervisor_tools.py`）：从 expert_graph 内联字典提取，固定顺序、注释禁止重排改写（工具定义是静态前缀的一部分）。
- **收尾不切客户端**（v8.4）：max-turns/熔断强制收尾统一保持带工具客户端 + HumanMessage 指令约束，请求载荷结构一致（此前切换无工具客户端导致同会话载荷结构来回变）。
- **缓存命中率观测**（`core/cache_metrics.py`）：从 `usage_metadata`/`response_metadata` 提取 DeepSeek `prompt_cache_hit/miss_tokens`，推送 `context_usage` SSE 事件（含 cache_hit/cache_miss/cache_ratio），每 100 次调用输出聚合命中率日志——静态前缀改造前后的一线对比指标。

### 2.3 提示工程（对应书 2.4）

- **结构化提示**：system 四个文件分工——`role.md`（角色与工作范围 8 项）、`constraints.md`（五段核心约束）、`decision_guide.md`（流程驱动式决策原则，含"文件保存必须调用工具"对照表）、`light_rules.md`（轻量模式边界）。XML/Markdown 双层结构（`<user_query>`、`<context>`、`<agent_status>` 等语义标签）。
- **流程驱动 vs 规则堆砌**（书 2.4.3）：`decision_guide.md` 给出"工具调用前思考 4 问 + 8 条行动规则 + 检索策略 + 深度问题生成策略"的标准操作流程；子 Agent 提示词（retrieve-agent 三阶段固定流程）是更进一步的 SOP 化。
- **业务规则细化**（书 2.4.4）：来源标注 5 级（RAG→DOI、学术库、联网、百科、`[模型知识]`）；文献冲突并列呈现；非柑橘证据标「类比：非柑橘证据」且占比 ≤15%；跨会话记忆冲突时"以本次检索证据为准"。
- **Few-shot 示例**（书 2.4.5）：decision_guide 用正反例对照表（如"保存到 output/1.md"→ 调 write_local_file ✓ / 调 call_write_agent ❌）。
- **提示注入防御**（书 2.4.7）：见 1.8 五层防御。

### 2.4 动态提示词与 Agent Skills（对应书 2.5）

**384 个段落骨架技能库**（`src/skills/` + `core/skill_tree.py`）——本项目最独特的领域资产：

| 分类 | 数量 | 内容 |
|---|---|---|
| abstract 摘要 | 73 | 摘要段"逻辑骨架 → 段落公式 → 衔接装置 → 原文对照"四段式 |
| intro 引言 | 39 | 引言/背景骨架 |
| discussion 讨论 | 109 | 讨论/结论骨架 |
| results 结果 | 9 | 结果段骨架 |
| contrastive 对比 | 18 | 对比式骨架 |
| methods 方法 | 3 | 材料与方法模板 |
| other 其他 | 133 | 无法归段的正文骨架 |
| **合计** | **384** | 按 section × rhetorical_move × journal_tier（Mid/Top）组织 |

- **渐进式披露实现**（对应书 2.5 三层结构）：一层= `skills_index.json` 元数据（name/section/domain/keywords/preview）；二层= 技能全文 md（write-agent 被调用时按任务 goal 向量检索 top-5 注入）；三层= 原文对照（md 内嵌）。检索用 e5-large 向量（文本= name+section+rhetorical_move+domain+keywords+preview 拼接），余弦阈值 0.4，MD5 向量缓存。
- **35 张策略卡片**（`core/strategy_cards.py`）：检索 10/规划 7/输出 10/审查 8，与 SkillTree 共用向量空间（阈值 0.35），按 query 语义匹配注入——"策略与技能分离、统一按需注入"。
- 技能来自真实论文语料的段落抽取（source_paper 字段可溯源），是"数据驱动的写作风格/结构迁移"而非人工规则。

### 2.5 Agent 状态栏（对应书 2.6）

`<agent_status>` 每轮以 user 消息追加到上下文末尾（`expert_graph.py` `_build_status_content`）：

```
当前轮次 / 已执行工具调用与每轮预算 / 已检索去重文献数 / 已用检索关键词 / 连续工具失败 / TODO 列表 / 上下文占用率
```

遵循书 2.6 的三条经验：
1. **代码维护，不用 LLM 维护**：状态内容全部由 supervisor 循环变量确定性计算（纯函数可单测，`tests/test_status_bar.py` 断言计数与真实值一致）。
2. **不删原始上下文**：状态栏与原始轨迹并存，只做显式投影。
3. **准确率作为生产指标**：单测覆盖 TODO 勾选状态、计数一致性、预算占用率格式。
- 注入方式为书 2.6.4 的**实现二（持久追加）**：本轮内只追加、不删除旧状态消息，状态小（~150 tokens）且更新频繁，符合书中的选择模型。
- 效果：模型无需从长历史"数"工具调用次数（"数不清"曾是预算超支的根因，F8 症状）。

### 2.6 上下文压缩策略（对应书 2.7，v8.4 架构重构）

**核心架构：存储全量 · 发送裁剪**（书 2.7.3 压缩与 KV Cache 互补的落地）：

- **存储层**：SQLite messages 表 append-only，原始轨迹（含 tool_calls/ToolMessage 配对）永不删除——压缩绝不写回历史（`replace_history` 已废弃）。
- **发送视图**（`context_manager._build_send_view`）：`[首消息] + <conversation_summary>(checkpoint) + [checkpoint 之后的原始消息]`；checkpoint（`sessions.checkpoint_msg_id/summary`）只记录"已摘要到哪条消息"，增量复用，防摘要套摘要。
- **触发时机**：**只在用户轮边界**（新请求 load 时）压缩；循环内绝不压缩（前缀中途变化 = 缓存必不命中，KV Cache 铁律写进注释）。
- **预算与阈值**：发送视图预算 = 模型窗口 1M（v8.4.3 由 512K 上调）；soft 0.75 / hard 0.93。
- **批量压缩至 ~50%**：触发时一次性压缩所有旧轮次至 target_ratio 0.50，不逐步挤压（每次压缩 = 一次缓存破坏，批量 = 只破坏一次）。
- **保护名单**（书 2.7.6 保留优先级）：最近 3 轮 Q/A 原样保留；被引用证据全文、DOI/evidence_id/artifact_id 标识符由压缩提示词保证保留；只压旧工具过程、错误日志、低相关证据。
- **上下文感知压缩**（书 2.7.5，实验 2-10 最优策略）：压缩提示带 `Given the user query` + 已有摘要作为 `prior_summary` 增量整合（git-log 式而非 git-squash 式）；24K 字符透明分片（`[上接前文/下接后文]` 标记，绝不静默截断）；`max_tokens` 真正传入 LLM。
- **分层压缩**（书 2.7.4 五层裁剪为四层）：L0 工具结果 offload 磁盘留引用（registry）→ L1 噪声删除（`_trim_noise` 删 `[ERR_*]`/占位消息并同步修剪配对 tool_calls）→ L2 批量上下文感知摘要 → L3 硬阈值规则式保护截断（`_hard_trim` 只留标识符行 + `[TRUNCATED]` 透明标记）。
- **熔断器**（书 2.7.4 L3）：连续失败 ≥3 降级规则式，避免在压缩失败的会话上持续烧钱。

---

## 第 3 章 用户记忆与知识库 → 本项目解剖

### 3.1 用户记忆系统（对应书 3.1）

**记忆层次**（书 3.1.2）：

| 层次 | 本项目 |
|---|---|
| 轨迹（当前会话） | messages 表 append-only 全量持久化（真相源） |
| 用户长期记忆（跨会话） | `ltm_facts`（ADD-only）+ `resident_cards`（常驻卡片）双层 |
| 会话内工作记忆 | entity/concept/preference 三类 JSON 存储（memory_store 表） |

**双层记忆架构**（书 3.3.5 实验 3-11 结论的落地）：

- **概览层（常驻卡片）**：高置信度（≥0.8）且短小（≤60 字符）的事实进 `resident_cards`，每次请求注入 ≤500 字符 `<resident_cards>` 块；上限 8 张、超限按"最低置信度 + 最旧"淘汰——**代码确定性维护，非 LLM**。
- **细节层（按需召回）**：`_recall_semantic` 向量检索，输出带 `[置信度] 事实 (更新: 日期 来源: 查询)` 标注，1500 字符上限，注入 `<long_term_memory>` 块。

**生命周期**（书 3.1 四步伪代码）：

1. 回答时召回相关记忆（`ContextManager.load`）；
2. 后台提取候选（回答 >500 字符且含实质结构时，`spawn` 后台用 FAST_MODEL 提取 ≤5 条"不可推导的核心事实"，不阻塞响应）；
3. 核验门槛：置信度 <0.5 拒绝写入；
4. **ADD-only 更新**（Mem0 v3 模式）：同 key 多版本并存，绝不覆盖/删除历史；冲突留到检索时用**混合信号排序**解决——`排名 = 语义相似度 × 置信度 × 时间衰减(0.95^天)`，旧版本自然沉底、冲突版本都返回由模型裁决（对比 Mem0 v2 写时 UPDATE/DELETE 的不可逆丢失）。

**一致性护栏**：任何记忆冲突时"以本次检索证据为准"（constraints.md）；记忆内容标注来源与更新时间。

**实体记忆**：正则实体词典五类（病害/品种/化合物/基因蛋白含 Cs 前缀/数值指标）+ 同句共现启发式关系抽取。

**证据账本**（`session_evidence`）：每轮检索报告 + 结构化清单（doi/chunk_id/title/score/摘要）持久化；下一轮以 `[历史检索证据]` 块注入，配合"证据回顾优先"决策（decision_guide）减少重复检索——chunk_id 可回查原文（INV-09）。

### 3.2 RAG 基础（对应书 3.2）

**分块**（书 3.2.1）：语料按论文分 chunk（JSONL），chunk 携带 paper_id/chunk_index/section_name 元数据——section_name 相当于轻量版"上下文感知检索"（书 3.3.5）的结构锚点。

**稠密嵌入**：e5-large（fastembed），query 加 `"query: "` 前缀；多批次 Qdrant local 向量库。

**稀疏检索**：**自研 BM25**（`retrieval/bm25.py`，~69 行）——k1=1.5、b=0.75、delta=1.0，IDF 平滑公式 `log(1+(N-freq+0.5)/(freq+0.5))`，含长度归一化；分词为英文词元 + 连续中文字串（丢弃单字）。如实记录：无倒排索引（每查询全语料线性扫描）、类名 BM25Plus 与实际公式（分子 tf+delta×tf）有出入——实现透明可审计但有性能天花板。

**混合检索**（书 3.2.4）：**加权三路 RRF**——orig_dense / hyde_dense / bm25 三路权重独立进 config.yaml，`score = Σ w/(k+rank+1)`，HyDE 路失败自动退化为双路等权。

**神经重排序**：bge-reranker-v2-m3 ONNX 本地推理（cross-encoder），sigmoid 归一化；DML GPU 全局互斥锁（Windows 单 context 限制）+ UnicodeDecodeError/运行时异常自动 CPU 降级重试。

**HyDE 工程化**：FAST 模型生成假想答案（提示词明确"禁止编造 p 值/基因 ID/accession 号"），MD5 LRU 缓存（500 条）、15s 超时、<30 字校验失败、任何异常静默降级为不启用 HyDE 的基础双路检索。

**动态阈值**（创新点 1，详见第七章）：

```
final_threshold = max(绝对下限 0.25, top-1 分数 × 0.60)
```

双保险设计：榜首置信度高时自动收紧（只留"与第一名同量级"候选），榜首分数低时钉在绝对地板。配合：
- **阈值拦截与"无匹配"分离归因**：`last_empty_reason ∈ {threshold_blocked, no_match}` 回传 LLM，让模型"对症下药"而非盲目换词；
- **决策审计日志**：每次过滤把 PASSED/FILTERED 两栏连同分数逐 chunk 落盘（`logs/debug_filter/`），检索行为可回溯；
- 全部被拦截时自动回退基础检索（HyDE 路）或触发模型知识兜底（标注 `[模型知识]`）。

**检索质量保证**：启动时抽样 200 点校验 `(paper_id, chunk_index) → 全局下标` 映射匹配率（<95% 告警）——Qdrant 随机 point id 不能当索引用的系统性坑（AG-11 修复）；多批次 lock 治理（陈旧锁清理/瞬时重试/冲突批次自动 BM25-only）。

### 3.3 超越扁平文本（对应书 3.3）

- **结构化知识组织**：384 段落骨架库即"按写作结构组织的知识"（RAPTOR 树状思想的领域化变体：section 为层、rhetorical_move 为聚类、原文为叶）。
- **证据账本**：检索报告的轻量结构化索引（doi/chunk_id 可回查）。
- **知识更新**（书 3.3.3）：轨迹 append-only（原始证据层）；LTM ADD-only + 常驻卡片淘汰（知识层）；无 Proposer-Reviewer PR 流程（单用户内网形态，属未来演进方向）。

---

## 第 4 章 工具 → 本项目解剖

### 4.1 分类总览（对应书 4.1）

见 1.1 表格。缺**事件触发工具**（无定时器/外部事件唤醒，异步仅在请求生命周期内）。

### 4.2 工具设计原则（对应书 4.2）

- **粒度权衡**（4.2.2）：按"功能相似 + 使用场景重叠"整合——read_local_file 统一多格式（PDF 双引擎/Excel/CSV），statistical_analysis 统一九种检验（对比"每检验一工具"）。
- **描述艺术**（4.2.4）：工具描述写成**决策条件**而非功能列表——如 call_write_agent 描述："Do NOT use when the content ALREADY EXISTS…use write_local_file directly instead"；read_local_file："Use FIRST when user asks to read/open/view a local file…For academic paper content extraction, prefer pdf_read"。参数描述带具体示例（路径格式、默认值语义）。
- **参数保真**（4.2.5）：绝不静默转换/注入——截断一律透明标记（`[... 已截断 ...]`、`[上下文已截断: 共 N 条内容，仅显示前 M 条，其余未提供]`），模型永远知道"自己没看全"（INV-08，F9 病史）。
- **统一结果头协议**：`## [ToolResult] 工具名 / status / query / results_count / elapsed_ms`，LLM 侧格式一致。
- **错误分类体系**：`[ERR_PARSE/ERR_NETWORK/ERR_TIMEOUT/ERR_FILE_TOO_LARGE/ERR_PERMISSION/ERR_HITL_REJECT/ERR_UNKNOWN_TOOL]` + 原因 + 建议。

### 4.3 工具生态（对应书 4.3）

未采用 MCP：LangChain `@tool` 装饰器 + config.yaml `tools.registrations` 注册（7 个工具规模下全量注入合理；MCP 的标准化收益在工具数量 >几十时才有价值）。如实记录：`init_tool_registry` 未被调用，concurrency_key 设计实际退化为按工具名分组（见 8.2）。

### 4.4 感知工具（对应书 4.4）

- `citrus_rag_search`：见 3.2 全链路；空结果三态归因协议（真无结果/网络错误/向量库批次降级）翻译成给 LLM 的行动建议。
- `academic_search`：四源并发（每源独立超时 5s）、查询预处理（停用词抽词 + 剥离 citrus/fruit/plant 泛词 + 末尾回加 `citrus` 领域限定）、DOI+标题去重、**柑橘相关性过滤防死循环**（全被过滤时强制保留 top3 并标注「非柑橘相关，仅供参考」，避免"0 结果 → 换词重试 → 再 0 结果"空转）、按引用数排序、源分布统计。
- `pdf_read`：PyMuPDF block 级过滤（页眉页脚/水印/图片）、Abstract/Results/Discussion/Conclusion 区间定位、近重复行去重、引文样板清洗（MDPI Received/Accepted 等）——结构感知而非全文倒出。
- 网络质量过滤：SPAM/撤稿/期刊目录页模式 + "Citrus County" 等 24 条地理歧义黑名单 + Nature/Science 等权威域名白名单。

### 4.5 执行工具（对应书 4.5）

- `write_local_file`：workspace/output 白名单、tmp+`os.replace` 原子写、路径分桶锁（防 read-modify-write 丢失更新）、append 模式预览显示"本轮新增块"。
- `statistical_analysis`：pandas/scipy 九种方法（描述统计/t 检验三变体/ANOVA/卡方/两种相关/线性回归），alpha 显著性标注，100000 样本量上限透明截断。
- `experimental_design`：四种设计（完全随机/随机区组/析因（效应量折半）/重复测量（×0.7）），scipy 功效估算双样本 z 近似。

### 4.6 协作工具（对应书 4.6）

- supervisor 通过 3 个 `call_*_agent` 工具调度子 Agent；子 Agent 各自独立上下文与工具面（retrieve：RAG+学术；write：写文件；analyze：统计+设计）。
- **子 Agent 上下文隔离**（详见 5.2，本项目与书 2.7.7 的差异化核心）。
- 每轮工具预算 ≤2 + 同轮至多 1 个检索子代理（防冗余决策）。

### 4.7 事件驱动的异步 Agent（对应书 4.7）

**SSE 事件协议（14 种）**：`status / thinking / tool_call_start / tool_executing / tool_result / tool_progress / agent_switch / agent_summary / text / citations / context_usage / context_status / log_line / done / heartbeat / error`。

- **心跳**：5s 空转保活；工具执行 >2s 时周期推送 `tool_executing` 进度（计时器严格配对，防事件风暴，F7 病史）。
- **队列式事件处理**（书 4.7.6 三策略之一）：事件进 per-request 队列，安全点批量消费；无取消式/并行式处理（同步 ReAct 模型约束，如实）。
- **断连保活双轨制**（创新点 9）：write 类任务（调用了 write 工具）断连时 graph_task 转交后台 `adopt` 继续执行，事件丢弃防队列堆积，完成后查 `task_jobs` 表；普通问答取消。
- **用户沟通**：`done` 事件携带 answer/gen_time_ms/tools_called/citation_info；前端单行瞬时状态（opencode 风格）+ 终端日志面板。

### 4.8 主动工具发现（对应书 4.8）

未实现——7 个工具全量注入合理（书 4.8 针对成百上千工具规模）；记录为演进方向（工具规模增长时按"静态前缀只留名称简述 + schema 追加轨迹末尾"实现渐进披露）。

---

## 第 5 章 Coding Agent 与通用 Agent → 本项目解剖

### 5.1 定位

通用科研 Agent（非 coding）：以"文献证据 → 结构化回答/综述"为核心任务，文件操作是**产物落盘**而非代码编辑。

### 5.2 隔离代替压缩：与 Coding Agent 的本质差异（核心创新点）

书 2.7.7 的"隔离优于压缩"针对 coding 场景：子 Agent 扫描代码库，中间数万 token 原始代码是**噪声**（找到目标函数后就无用），只回传几百 token 结论。若把该模式机械搬到检索→写作链路，会产生"双重 token 消费"：证据原文在检索子 Agent 筛选时 prefill 一遍，写手写作时又要重新检索、再 prefill 一遍。

**本项目的差异化方案：隔离"过程"、传递"产品"。**

| | Coding Agent（书 2.7.7） | 本项目（检索→写作） |
|---|---|---|
| 子 Agent 中间产物性质 | 噪声（扫描的代码） | 过程（检索词迭代/失败重试）+ 产品（证据 chunk） |
| 隔离对象 | 全部中间产物 | 仅过程（子 Agent 轨迹随 run_agent 返回即弃） |
| 产品传递 | 几百 token 结论 | **artifact 进程内传递（零 token）**：chunk 全文走 `content_and_artifact` 的 artifact 字段，不经过模型上下文 |
| 写手获得 | 结论 | 证据全文（material_pack 结构化对象直传写流水线） |

因此：中间噪声的隔离收益拿到了（子 Agent 临时上下文天然绝缘，主上下文只进检索报告），而证据本体只被写手 prefill 一次——**不产生双重消费**。这是"隔离优于压缩"思想在"证据即产品"场景的适配。

### 5.3 Write Pipeline：Plan-Execute 工作流

**四路路由**（`classify_write_task`）：文件存在 → modify（定向改章节）；"保存/存入"且非撰写意图 → direct_write（原样落盘）；"综述/报告/多章节" → plan_execute；"草稿/先写/看看风格" → react。正则快筛覆盖 80% 明确场景 + LLM 结构化分类兜底（失败保守回退 react）。

**Stage 1 Plan**：只输出严格 JSON 大纲（title/abstract_draft/sections[{heading, points, target_chars, refs}]）；动态阈值校验双模式（指定字数严格/自主模式宽松：章节数、单章 250-3000 字防截断、总字数、refs 覆盖 ≥40% 章节）；失败带失败项重试，彻底失败降级 ReAct 一次性生成并落盘。

**Stage 2 Execute**：逐章独立 LLM 调用（每章预算 100% 聚焦一章）；章间上下文 = 前章 `<summary>` 隐藏标签（代码提取、只留 500 字符，非累积式）；材料按 refs 语义分配（DOI 精确 → 标题模糊 → 前 3 篇兜底，未匹配 refs 显式反馈"不得虚构文献支撑"）；写后 read-back 校验（标题双向比对）；非 LLM 正则引用完整性校验（正文 [n] vs 文献区 [n] 双向差异）。

**断点续传**：`pipeline_tasks` 表持久化 Plan/已完成章节/材料包（免重新检索），`BEGIN IMMEDIATE` 防并发丢标记；完成状态 done/partial 区分；缺章占位优雅降级。

### 5.4 文件操作机制（与 Coding Agent 的差异）

- 长文写作用 **write + append 续写**（每轮 1-2 章、单轮 12000 token 上限），而非 edit 式 diff 编辑——与"多轮生成式写作"任务匹配。
- 写后闭环：read-back + 引用完整性 + 材料 gap 透传（`material_gap` 全程告知缺文献支撑的章节）。

---

## 第 6 章 Agent 评估 → 本项目解剖（如实）

### 6.1 评估体系（对应书 6.2/6.3）

**10 条工程不变量（INV-01~10）+ 症状库 F1-F9 + 测试映射**——每条不变量有对应回归测试：

| 不变量 | 一句话 |
|---|---|
| INV-01 协议配对 | tool_calls 与 ToolMessage 严格配对，id 单一来源 |
| INV-02 生命周期有界 | 轮数/预算上限，收敛由代码裁决 |
| INV-03 检索降级 | 向量→BM25→直答 fallback 链 + 归因 |
| INV-04 上下文传播 | contextvar 贯穿线程 + 计时器配对 |
| INV-05 输出路由 | 子代理输出约束 + 深度问题结构化链 |
| INV-06 可观测回归 | 事件词汇表前端可消费 + request_id |
| INV-07 状态显式化 | `<agent_status>` 注入，禁止模型自行"数" |
| INV-08 失败熔断与输入隔离 | 连续失败熔断 + 透明截断 |
| INV-09 证据账本 | 检索报告/清单持久化 + 跨轮复用 |
| INV-10 存储全量·发送裁剪 | 轨迹 append-only + 发送视图可裁剪 |

**测试体系**：11 个测试脚本、约 351 项断言（源码断言 + mock 行为验证）；3 个端到端脚本——`verify_retrieval.py`（真实检索验收：3 真实查询、命中 30、0 错位、去重 ≥2）、`start_check.py`（启动健康 + idx_map 自检）、`simulate_flow.py`（不调 LLM 的推演模拟：分块写入/容量/收敛场景）。

### 6.2 过程指标（对应书 6.2.3）

工具调用计数、子 Agent 轮数与耗时、gen_time_ms、token 增量（SSE context_usage）、**前缀缓存命中率**（cache_metrics，每 100 调用聚合）、检索决策审计日志、空结果率归因（threshold_blocked/no_match）。

### 6.3 自动化评估现状（对应书 6.3/6.5，如实）

- **LLM-as-judge：未实现**（生产路径中仅 `_classify_document` 是功能分类器，非评测器）。
- **基准测试/评测集：无**。
- **人工复测清单**：7 项（AGENT_CHANGES 第 5 节）——深度问题收敛行为、锁争抢降级、预算截断无 400、心跳量级、状态栏可见、write 流水线落盘、协议配对。
- 差距与路线图见 8.3。

### 6.4 可观测性（对应书 6.8）

SSE 事件协议 + 前端面板（上下文用量抽屉：总量/剩余/软硬阈值/明细占比）、request_id 贯穿日志（RequestIdFilter）、启动 ERROR 汇总、检索日志、工具心跳计时器、状态栏、`GET /jobs/{id}` 任务查询、done 事件附 citation_info（引用无支撑/数量异常前端轻提示）。

### 6.5 模型选择（对应书 6.6）

MAIN/FAST 双路由（见 1.3 与 MODEL_ROUTING.md）；模型可运行时切换（switch_model）；温度策略：生成 0.2（事实准确性优先）、辅助 0.0（确定性）、HyDE 0.2（甜点值）。

---

## 第 7 章 创新点汇总（供汇报/论文直接引用）

1. **双保险动态阈值**：`max(绝对下限 0.25, top-1 分数 × 0.60)` 的相对自适应 + 绝对地板，配合阈值拦截/无匹配分离归因与逐 chunk 决策审计日志——检索阶段的自适应质量控制，而非固定阈值一刀切。
2. **隔离代替压缩的科研适配**：区分子 Agent 中间产物的"过程 vs 产品"——隔离过程（轨迹随子 Agent 返回即弃）、artifact 进程内传递产品（证据零 token 到达写手），避免 coding Agent 模式的"双重 token 消费"，同时保留主上下文噪声绝缘的全部收益。
3. **存储全量 · 发送裁剪 + checkpoint 增量压缩**：append-only 原始轨迹 + 发送视图（checkpoint 摘要 + 后续原文）分离；用户轮边界批量压缩至 50%（缓存只破坏一次）；上下文感知压缩（带 query + prior_summary 增量整合）防摘要套摘要；保护名单（决策/被引证据/标识符）显式保留。
4. **ADD-only 记忆 + 双层记忆**：Mem0 v3 模式（写时绝不覆盖、冲突留到检索时用"语义相似度 × 置信度 × 时间衰减"排序解决）+ 常驻卡片概览层（代码维护、≤500 字符）+ 按需召回细节层。
5. **384 段落骨架技能库**：从真实论文语料按 section × rhetorical_move 抽取的"逻辑骨架 → 段落公式 → 衔接装置 → 原文对照"四段式技能，向量检索按需注入——数据驱动的写作结构与风格迁移。
6. **错误即提示词 + 透明截断文化**：`[ERR_类别] 原因 + 建议` 机器可读错误协议（模型可对症修正）；一切截断显式标记（模型永远知道"没看全"）。
7. **代码级收敛**：检索充分性由代码裁决（去重文献 ≥6 强制收尾），与提示词软约束双保险，模型意愿不得绕过——治理"跑满轮次/重复检索"类失效。
8. **空结果三态归因协议**：真无结果 / 阈值拦截 / 向量库降级三种空手原因显式回传 LLM，配对应行动建议，杜绝静默空手与盲目换词。
9. **断连保活双轨制**：write 类任务断连转后台继续执行（事件丢弃防堆积、任务表可查），普通问答取消——长任务可靠性与交互延迟的分治。
10. **工程不变量体系**：INV-01~10 + 症状库 + 测试映射的"护栏式"Agent 工程治理——每个失效模式有病史、有修复、有回归测试。

---

## 第 8 章 现状差距与路线图（如实记录）

### 8.1 已声明的安全防护部分未接线

- **HITL**：UI/端点/settings 齐全但后端 stub（`/api/v1/hitl/pending` 恒返回空）、`HITL_DANGEROUS_TOOLS` 空集合、auto_approve 仍为测试模式 true——"声明存在、实际未接线"。
- **沙箱不对称**：fail-closed 检查存在，但 `init_tool_registry` 未被调用导致注册表空转，写工具"经子 Agent 节点被拒（write_* 模式）、经 supervisor 直写路径放行"的不对称。
- **offload 清理**：`cleanup_offload_files` 从未被调用，tmp 文件累积。

### 8.2 配置与实现漂移

- `TUNE_PARAMS.md` §5 上下文预算（1M/0.60）与 config.yaml（512K/0.75）已漂移；README 中部分配置片段为旧值。
- `BIO_DB_CACHE_TTL` 死配置；`call_specialist` 幽灵注册项；`feature_flags` 部分开关无消费点。
- BM25 无倒排索引（每次查询全语料线性扫描）；`BM25Plus` 类名与实际公式有出入（诚实记录，非 bug 声明）。

### 8.3 评估体系差距

- LLM-as-judge 缺失、无基准测试集、无 golden-trace 回放（AUDIT 明确排除）。
- rerank 阈值未校准（AG-13 跳过）；Embedding/Reranker 结果缓存缺失（TUNE_PARAMS §9 ❌）。

### 8.4 路线图（按价值排序）

1. **评估基建**：构建柑橘领域问答评测集（真实 query + 标注证据）+ LLM-as-judge 打分管道——论文/汇报最需要的数据支撑。
2. **检索质量实测**：50 条真实 query 的 rerank 分数分布标注 → 校准动态阈值参数与 RRF 权重（TUNE_PARAMS 已有 SOP，缺执行）。
3. **HITL/沙箱收尾**：注册表初始化接线、write 类操作统一审批路径（或显式声明单用户内网豁免）。
4. **缓存补全**：query embedding 缓存（LRU）、rerank 批处理。
5. **静态前缀灰度转正**：`context.static_prefix` 灰度验证后切默认 true，量化缓存命中率收益。
6. **工具渐进披露**：工具规模增长时按书 4.8 模式演进（名称简述常驻 + schema 追加轨迹末尾）。

---

## 附录：文档索引

| 文档 | 定位 |
|---|---|
| README.md | 快速开始 + 执行流程（部分配置片段待同步 v8.4） |
| readme2.md | v8.3.0 历史架构手册（SSE 协议全表） |
| AGENT_CHANGES.md | 工程不变量（INV-01~10）+ 症状库 + 测试映射 + 变更历史 |
| AUDIT_2026-08-10.md | 生产就绪度体检（22 项问题及处置） |
| TUNE_PARAMS.md | 50+ 参数清单 + 调优 SOP（§5 待同步） |
| DEPLOY.md | 部署指南 + 部署后验证 |
| MODEL_ROUTING.md | 模型路由矩阵（v8.4） |
| 本文档 | 对照《深入理解AI Agent》的章节式开发现状解剖 |
