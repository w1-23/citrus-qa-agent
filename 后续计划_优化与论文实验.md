# 后续计划：项目优化 + 论文实验 + 实验实现方案

> 整理时间：2026-08（v8.11 之后）。
> 合并来源：① `未来建议待办.md`（B/O 待办日志）② `待完成.doc`（消融框架/问题集/工具演进）③ 上一轮 `待完成.doc` 第五~七节（内存/审计/书审计 16 项）。
> 依据书：李博杰《深入理解 AI Agent：设计原理与工程实践》，关键章节 §2.7 压缩、§3.3 进阶 RAG、§6 评估、§8 持续进化、§10.5 多 Agent 失败模式。
> 本阶段原则：**先不做，只整理计划**。以下每项均标注「现状 / 阻塞点 / 实现方式」。

---

## 一、项目本身可做优化（产品侧，非论文）

> 这些是对产品本身的改进，独立于论文；多数在改完后还能作为论文的「消融组/特性」复用。

### A. 检索质量直接改进（建议优先，能反哺论文）

| # | 项目 | 依据 | 现状 | 备注 |
|---|---|---|---|---|
| A1 | 上下文感知检索 Contextual Retrieval | 书 §3.3.5 | 待做 | 索引期为每个 chunk 生成「该块在全文中的定位」前缀再嵌入。检索失败率 ↓49%（BM25）/↓67%（叠加重排，Anthropic 数据）。注意：B7 已做的是**查询侧** previous_queries 注入，本条是**索引侧** chunk 语境，两者互补不重复。 |
| A2 | 篇级/主题摘要索引（结构化索引轻量版） | 书 §3.3.1 | 待做 | 综述写作需跨文档聚合，扁平 chunk 会漏。先做「篇级摘要」随 chunk 同批入库；RAPTOR/GraphRAG 后续（重）。 |
| A3 | 检索源全开三待定 | 未来建议「四·一」 | 待定 | ①跨源合计≤8 条上限 ②摘要 500→300 字 ③前端显示启用源。跨源去重已存在，全开即可用，风险是延迟 +3~8s。 |

### B. 健壮性 / 成本 / 闭环

| # | 项目 | 依据 | 现状 | 备注 |
|---|---|---|---|---|
| B1 | proposer-reviewer 校验闭环 | 书 §10.5.3 / O3 | 待定 | 回答生成后 rubric 审核，不达标重生成 1 次。评估类此前暂缓。 |
| B2 | 知识更新审核门（增量 vs 全量） | 书 §3.3.3 | 待做 | 语料翻三倍前必做：入库审核、去重、脏数据拦截，ingest 由追加式升级为审核式。 |
| B3 | 交叉验证防错误级联 | 书 §10.5.2 | 待做 | 写作发布前校验「引用↔证据」一致，不看思考过程只看证据；呼应证据全文主线。 |
| B4 | LTM 定期整理（同义聚类合并） | 书 §8.3.5 / O9 | 待做 | embedding 相似度 >0.95 聚类合并新版本行。 |
| B5 | 子代理 token 预算告警 | 书 §10.5.3 / O11 | 待做 | 日计数超限跳过+告警。 |
| B6 | evidence 折叠分支 | O1 | 保留·分支优化 | 观察 token 成本后再决定是否 feature flag（折叠式注入）。 |
| B7 | 分层压缩补齐 + 80% 占用演练 | 书 §2.7.4 / 观察项① | 待做 | 1M 窗口下压缩路径缺实战，评估集建立后做一次 L3 熔断演练。 |
| B8 | 多 Agent 乐观锁 | 书 §10.5.1 | 已具备基础 | claim_resumable_task 互斥、write_calls 串行已覆盖；若放开并行写草稿再加版本号。 |
| B9 | 死代码清理 | 观察项② | 待做 | `build_human_message` 的 `<conversation_summary>` 死分支。 |

### C. 持续进化（书第 8 章）

| # | 项目 | 依据 | 现状 |
|---|---|---|---|
| C1 | 经验沉淀第二步（聚类→草案→确认进 skills） | 书 §8.2.1 / O7 | 第一步已做（feedback 表+👍👎），聚类/草案待做 |
| C2 | 三层轨迹验证（结果/过程/质量） | 书 §8.1 | 待做，产出结构化学习信号 |
| C3 | 系统提示学习（最小 diff 补丁） | 书 §8.2.2 | 待做；已有 snapshot 机制（O5）可承接 |

### D. 工具生态（`待完成.doc` 已论证）

| # | 项目 | 优先级 |
|---|---|---|
| D1 | 开放全文获取（Unpaywall / PMC OA / bioRxiv） | 高（与证据全文主线一致） |
| D2 | 网页读取 fetch URL | 高 |
| D3 | 参考文献格式化导出（GB/T 7714 / BibTeX） | 中（已有 _unify_references 可包装） |

### E. 多模态

| # | 项目 | 依据 | 现状 |
|---|---|---|---|
| E1 | PDF 图表感知 | 书 §9 / O12 | 待定，依赖视觉模型开放后 |

---

## 二、项目论文需做实验（论文证据链）

> 对应 `待完成.doc` 的「主线 A2 四组实验」+ 本书 §6 方法论强化 + 内存效率新增。按实验编号组织，含书出处与所需资产。

### E1 检索质量主实验（RAG 流水线基线 vs 各组件）
- 指标：recall@k / MRR / nDCG / 检索失败率（书 §6.2、§3.2）
- 内容：稠密 vs BM25 vs 混合(RRF) vs 重排（逐级叠加看增益）
- 资产：questions.jsonl + gold_dois

### E2 消融实验（§6.10.1）
- 逐关：HyDE / BM25 / rerank / 记忆 / 偏好注入 / 单 vs 多 Agent / 证据账本 / 状态栏
- 资产：开关点（已现成 ~9 项 + 待补 6 项）+ 跑批脚本

### E3 证据策略对比（核心创新）
- 证据全文 vs 摘要；免重检 vs 重检（追问对测复用）
- 资产：证据全文/摘要开关 + 证据账本开关 + followup 对

### E4 成本实验
- token / 耗时 / 检索次数 / cache 命中率（书 §6.6.3）
- 资产：跑批脚本自动收集（business.log 已埋 evidence_avail/cited）

### E5 效率实验（新增，内存优化入论文）
- 内存-语料规模曲线（旧线性 vs 新近平坦，20k/60k/120k/360k chunks）
- 懒加载延迟开销 + LRU 命中率
- 资产：语料子集 + RSS 采样脚本（方法已跑通 probe）

### E6 统计严谨性（§6.7、§6.2.2、§6.10.2）
- Pass^k 稳定性（每题每配置重复 r=3~5）
- 配对检验显著性（每组 ≥30 样本）
- 机制指标（检索次数/token）vs 目标指标（正确率/满意）+ 护栏指标（幻觉率不退化）

### E7 失败分析与边界回归（§6.5.2、§6.5.3、§6.1）
- 失败归因（首个错误定位）；轨迹前缀回归（专测「免重检」第二轮的账本复用）
- LLM-as-Judge Rubric，幻觉设一票否决（veto）

---

## 三、实验实现方案（能否脚本化 + 缺什么）

### 3.1 总体结论（直接回答）

1. **脚本都能写**：E1~E7 全部可以脚本化，我可以逐一实现。
2. **现在能不能跑**：脚本代码不阻塞；**阻塞点几乎全在「数据」，不在代码**：
   - 最高阻塞：**问题集 + 检索金标准（gold_dois）** —— 这是所有「跑问答/打分」实验的输入，目前缺失。
   - 其次：6 个消融开关点代码（每个 3~5 行，向后兼容）。
   - 可选：Contextual Retrieval 实现（若要 A1 的消融组，需 LLM 生成 chunk 前缀，有一次性成本）。
3. **独立脚本 vs 开关形式**：两类都要，各有所长（见 3.3）：
   - 纯测量型（E1 检索质量、E5 内存）→ **独立脚本**最合适（不经过 LLM 对话，快、便宜、确定性强）。
   - 需跑通完整问答多配置（E2/E3/E4/E6/E7）→ **开关 + 跑批脚本**最优（settings 进程级单例，每个配置一个子进程注入 env）。

### 3.2 现有可复用资产（已就绪）

- ✅ 消融开关：config.py 用 pydantic-settings（环境变量 > yaml > 默认），已现成维度：HyDE、静态前缀、上下文预算、RRF 三维权重、动态阈值、并行写作、单/多 Agent、学术源、沙箱权限。
- ✅ 跑批模式：settings 进程级单例 → 每个配置组一个子进程，天然隔离（`待完成.doc` 第三节已设计 run_ablation.py 结构）。
- ✅ 提示词版本快照：snapshots/*.txt（O5）。
- ✅ 埋点：business.log 的 evidence_avail/cited、检索预算/去重计数。
- ✅ 历史数据：sessions.db 183 个会话 + feedback 表（可挖真实问题）。
- ✅ 内存采样方法：probe 的 RSS 采样脚本已在本机跑通（用于 E5）。

### 3.3 逐实验实现方式与可达性

| 实验 | 形式 | 能否运行 | 缺什么 / 说明 |
|---|---|---|---|
| E1 检索质量 | 独立脚本 `eval_retrieval.py`（直接调检索器，不经 LLM） | ❌ 缺数据 | 缺 questions.jsonl + gold_dois；脚本本身能写、且最便宜。 |
| E2 消融 | 开关 + 跑批 `run_ablation.py` | ⚠️ 半就绪 | 需补齐 6 开关点 + questions.jsonl；开关大半现成。 |
| E3 证据策略 | 开关 + 跑批 | ⚠️ 半就绪 | 需补证据全文/摘要开关、证据账本开关 + followup 对。 |
| E4 成本 | 跑批脚本自动收集 | ✅ 基本可跑 | 埋点已有，仍需 questions.jsonl 才有输入。 |
| E5 内存-规模 | 独立脚本 `bench_memory.py` | ✅ 可跑 | 需切语料子集（可 ingest 切分）；方法已跑通。 |
| E6 统计 | 跑批加 `repeat=r` + 统计模块 | ✅ 可写 | 缺 questions.jsonl；统计用 scipy 配对检验。 |
| E7 失败分析 | 轨迹前缀回归独立脚本 + Rubric | ⚠️ 半就绪 | 缺 followup 对 + Rubric 定义（幻觉 veto 为配置项）。 |

### 3.4 缺失清单（按阻塞优先级排序）

1. **【最高】questions.jsonl 问题集**（100~120 题，四类：mechanism 25 / compare 25 / fact 25 / followup 20 / review 8）——schema 已在 `待完成.doc` 定义，可从 sessions.db 183 会话提取真实提问作为底稿。
2. **【高】gold_dois 检索金标准**（每题 2~5 条 DOI）——人工标注，或从历史会话引用自动提取 + 人工校验（100 题 × 2~5 分钟 ≈ 半天量）。
3. **【中】6 个消融开关点代码**——记忆 / 证据账本 / 状态栏 / 证据全文 vs 摘要 / 子代理禁用 / 重排开关（每个 3~5 行，默认值=生产行为，零回归）。
4. **【中】LLM-as-Judge Rubric 定义**——三维（事实支撑 / 引用一致 / 缺口声明）+ 幻觉一票否决，配置化。
5. **【低/可选】Contextual Retrieval 实现**——仅当要做 A1 消融组时才需要；批量 LLM 生成 chunk 前缀，有一次性 token 成本。
6. **【低】语料子集切分工具**——E5 用（20k/60k/120k/360k）。

### 3.5 成本与环境备忘

- 跑批成本：100 题 × ~8 配置组 ≈ 800 次完整问答，DeepSeek 量级约几美元（每题设 token 预算上限）。
- 环境：跑测试/脚本用 `E:\anaconda\envs\rag-agent\python.exe` 直接调用（conda run 有 GBK bug）；pytest 需 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` + `FASTEMBED_CACHE_PATH`；从 `agent/` 目录启动（reranker 缓存已绝对路径化）。

---

## 四、建议的落地顺序（供你审阅后定，本阶段不动手）

1. **第一阶段（数据准备，不写产品代码）**：从 sessions.db 挖 100 题 → 标注 gold_dois → 得到 questions.jsonl。此步是后续一切实验的前置。
2. **第二阶段（测量类独立脚本）**：先写 E1 检索质量 + E5 内存两个独立脚本（最便宜、最先出数据，且内存实验可立即跑、直接支撑论文效率小节）。
3. **第三阶段（开关补齐 + 跑批）**：补 6 个开关点 → 写 run_ablation.py → 跑 E2/E3/E4/E6/E7。
4. **可选增益（与实验并行）**：A1 Contextual Retrieval 实现后，作为一个消融组并入 E1/E2。

---

## 五、数据到位前可预先准备的工作清单（A/B）

> 原则：数据集（questions.jsonl + gold_dois）由用户收集，不阻塞以下任何准备。**当前一律不执行，仅列清单。**

### A 类：不动产品代码、可预先准备的（纯新增脚本/模板/工具）

| # | 产物 | 作用 | 依赖数据？ |
|---|---|---|---|
| A1 | `eval_retrieval.py` | 检索质量评测（recall@k / MRR / nDCG），直接调检索器不经 LLM | 需 questions.jsonl + gold_dois，骨架现在就能写 |
| A2 | `bench_memory.py` | 内存-语料规模曲线 + 懒加载延迟/命中率（E5） | 语料子集即可，方法已跑通，现在就能写并跑通 |
| A3 | `run_ablation.py` | 跑批框架：读矩阵 → 每配置一个子进程注入 env → 落盘结果 | 需问题集，框架骨架现在就能写 |
| A4 | `ablation_matrix.yaml` | 消融配置矩阵模板（开关组合 = cell） | 无 |
| A5 | `questions.jsonl`（schema + 示例） | 问题集字段模板 `{id, category, question, followup?, gold_dois}`，供收集时对照 | 无（仅模板） |
| A6 | `judge_rubric`（配置 + 判分脚本） | LLM-as-Judge 三维 Rubric + 幻觉一票否决 | 无（Rubric 定义即可） |
| A7 | `make_corpus_subset.py` | 切语料子集（20k/60k/120k/360k chunks） | 无（读现有语料切分） |
| A8 | 统计模块 | Pass^k 计算 + scipy 配对检验（E6） | 无 |

### B 类：需动产品代码的开关点（每个 3~5 行，等明确「开始」再动）

> 默认值 = 生产行为，向后兼容零回归；共 6 个，均为消融实验所需。

| # | 开关点 | 加在哪 |
|---|---|---|
| B1 | 记忆开关（LTM 召回 / 常驻卡片 / 偏好注入，各自独立） | `context_manager.load` 装配处 |
| B2 | 证据账本开关（历史检索证据注入） | `context_manager.build_evidence_block` 处 |
| B3 | 状态栏开关（`<agent_status>`） | `expert_graph` 状态栏注入点 |
| B4 | **证据全文 vs 摘要**（核心创新消融） | 证据报告截断 / 回执构建处 |
| B5 | 子代理禁用（变单 Agent ReAct） | supervisor 工具 schema 装配处 |
| B6 | 重排开/关 | `multi_retriever` 的 rerank 调用处 |

### 结论

- **A 类**：A2（内存脚本）现在就能写并跑通；其余 7 个为纯新增脚本/模板，不碰产品代码，可按顺序预制。
- **B 类**：6 个开关点要动产品代码，等用户明确「开始」再动。
- 数据集收集与以上所有准备完全并行，不互相阻塞。