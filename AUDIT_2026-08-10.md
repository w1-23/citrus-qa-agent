# Citrus QA Agent v8.3.0 生产就绪度体检报告

- 日期: 2026-08-10
- 范围: 会话隔离、上下文与记忆、工具执行与 ReAct、业务参数、可观测性
- 结论: 共 22 个真实问题（P0×2 / P1×2 / P2×13 / P3×5）
- 模型前提: DeepSeek V4 Flash，上下文 1M tokens（2026-08-11 修正：AG-3 原"超窗"论断基于 64K/128K 假设，与所用模型不符，已降级为配置问题）
- 修改范围约定: **第二章（智能体功能问题）本次拟修复；第一章（部署/生产级）仅记录存档，不修改**

> 已知问题（全局进度队列串流、ReAct 循环内无 token 预算检查）按约定不重复报告。

---

# 第一章 部署 / 生产级问题（仅记录，本次不修改）

## DEP-1 [P1] 无认证 API + CORS 全通 + 任意绝对路径文件读取
- **位置**: `agent/src/api/main.py:84-90`（CORS）、`main.py:93-131`（无鉴权）、`agent/src/tools/readfile.py:21-34`、`agent/src/tools/analyze.py:44-47`
- **现象**: 任何能访问端口的进程/网页无需凭证即可 POST `/api/v2/chat`，携带任意 `session_id` 读取该会话全部历史，或让 LLM 调 `read_local_file("C:/Windows/...")` 读取任意绝对路径文件并回显。
- **根因**: `read_local_file` 对绝对路径不做沙箱（`full = p.resolve()` 直接返回）；`statistical_analysis` 同理；API 层无认证中间件，`session_id` 即"口令"；`allow_origins=["*"]` + `allow_credentials=True`。
- **影响**: 机密文件外泄、任意会话历史被读取/污染。多用户部署即数据安全事故。
- **修复方案（留档，不实施）**: ① `read_local_file`/`statistical_analysis` 绝对路径收敛到 workspace 白名单；② API 加 Bearer Token 中间件；③ CORS 收敛为前端实际域名，`allow_credentials` 与 `"*"` 不能并存。
- **验证方法**: 修复后 `POST /api/v2/chat {"session_id":"victim","query":"读 C:/Windows/win.ini"}` 应返回 `[ERR_HITL_REJECT]` 或 401。

## DEP-2 [P1] "三级防御体系"实际落空：sandbox 模块缺失被静默放行、HITL 为 stub、断路器零实例化
- **位置**: `agent/src/tools/registry.py:107-117`、`agent/src/api/main.py:326-335`、`agent/src/guardrails/circuit_breaker.py`（全文件无调用点）
- **现象**: `src/engine/` 下不存在 `sandbox.py`，`from src.engine.sandbox import check_sandbox` 必抛 ImportError → `pass` → **每个工具调用实际零沙箱拦截**；`hitl.auto_approve: true` 默认放行；HITL 端点恒返回空；`CircuitBreaker` 类无任何实例化。
- **根因**: 防御链路未接线，注册表在 ImportError 时默认放行（fail-open）。
- **影响**: 新增危险工具（delete/exec）时无防线兜底；安全特性声明与实况不符。
- **修复方案（留档，不实施）**: `_check_tool_sandbox` 改 fail-closed（模块缺失即拒绝并 error 日志）；补 `engine/sandbox.py`；删除或实现 HITL stub；`hitl.auto_approve: false`。
- **验证方法**: 临时删除 sandbox 模块后调用任意工具应被拒绝而非放行。

## DEP-3 [P2] save 任务 fire-and-forget + 同会话并发无锁 → 历史丢失或交错
- **位置**: `agent/src/graph/expert_graph.py:671`、`light_graph.py:325`、`main.py:122-127`
- **现象**: 回复完成后进程立即退出（重启/崩溃），后台保存任务被 GC 销毁，本轮对话永久丢失且无日志；同 `session_id` 并发请求时两个保存任务先后 INSERT，历史重复/交错。
- **根因**: `asyncio.create_task(session_manager.save_messages(...))` 未持有引用、未 await；保存为 append 语义，无幂等键；无请求级写锁。
- **影响**: 断连/重启场景下上一轮上下文系统性丢失；并发场景历史错乱。
- **修复方案（留档，不实施）**: 模块级 `_SAVE_TASKS` 持有 Task 引用；保存加幂等唯一键；同会话写串行化（按 session_id 分桶 asyncio.Lock）。
- **验证方法**: 回复后 0.5s 内 kill 进程，重启后查询 messages 表应有该轮记录。

## DEP-4 [P2] 断连/重启即死：无任务持久化、无 interrupted 告知
- **位置**: `agent/src/api/main.py:287-303`、`expert_graph.py:671`
- **现象**: SSE 客户端断开 → `graph_task.cancel()` → save 节点不再执行，本轮问答既不落库也不落盘；服务重启时进行中请求全部死掉，用户无任何中断信号。
- **根因**: 图执行与 SSE 连接生命周期强耦合；无 job 表；保存仅在图末端一次性执行。
- **影响**: 长任务（综述写作 2-5 分钟）一次网络抖动 = 整轮工作丢失。
- **修复方案（留档，不实施）**: 客户端断开时不 cancel 图任务（仅停止下发事件）；`queries` 表记录 running/failed 状态，重启后返回 `last_interrupted` 供前端提示。
- **验证方法**: 模拟客户端 1s 断开，messages 表仍出现该轮记录。

## DEP-5 [P2] 配置与代码大范围脱节（死配置 / 相互矛盾清单）
- **位置**: `config.yaml` 多处 vs 代码硬编码
- **现象与明细**:

| 配置项 | 配置值 | 代码实际 |
|---|---|---|
| `supervisor.max_turns` | 8 | `SUPERVISOR_MAX_TURNS=4`（expert_graph.py:33） |
| `subagents.retrieve-agent.max_turns` | 3 | `_get_max_turns` = 1（agent_runner.py:293-299） |
| `agent.timeout_sec` | 30 | 实际 120（expert_graph.py:282） |
| `api.llm_timeout / intent_timeout / intent_max_tokens` | 15/10/10 | 无引用 |
| `guard.max_query_length` | 12 | main.py:48 硬编码 12 |
| `web_search.*`（serper 白名单） | 完整配置 | 无任何 web 工具实现 |
| `retrieval.web_relevance_threshold` | 0.3 | 无引用 |
| `circuit_breaker.*` | 90/95/8 | 无引用 |
| `context_budget.*` | 0.90/0.95 | 图内硬编码 0.85/0.93（配置单向化问题，非功能故障；窗口 1M 与模型对齐） |
| `graph.recursion_limit` | 25 | astream 未传 |
| `tools.max_tool_calls` | 4 | 无引用 |
| `feature_flags.*` | 多开关 | 仅 `light_mode` 被读，且 API 流不生效 |
| `hitl.timeout_sec` | 30 | `HITL_TIMEOUT_SEC` 默认 120 |

- **影响**: 调参"看起来生效实则无效"；retrieve 深度被隐性阉割（声明 3 轮实际 1 轮）。
- **修复方案（留档，不实施）**: 参数统一从 config → settings 读取；启动期 `config_audit.py` 校验输出 dead-key 报告。
- **验证方法**: 启动日志 `[ConfigAudit] dead keys: []` 为空。

## DEP-6 [P3] 可观测性小项
- **位置**: `main.py:346-348`（`/health` 返回 "8.1.1" 实际 8.3.0）、`main.py:46-50`（`"what can you do"`(15字符) 因 `len>12` 永不命中 Fast Guard）、`registry.py:21,92-104`（offload 临时文件永不清理，`cleanup_offload_files` 无调用点）、`progress_bus.py:53-55` + `main.py:237-249`（done 事件无 token 消耗/LLM 调用次数；路由决策无日志）
- **修复方案（留档，不实施）**: 版本号统一常量；guard 判断改 token 数；lifespan shutdown 调 `cleanup_offload_files()`；done 事件扩展 `tokens_used/llm_calls/tools_called`。

---

# 第二章 智能体功能问题（本次拟修复）

## AG-1 [P0] supervisor 截断分支引用未定义变量 `task` → 工具结果超限即整轮崩溃
- **位置**: `agent/src/graph/expert_graph.py:544-547`（`supervisor_node`）
- **现象**: 子 Agent 结果超过分档上限（write-agent >1000 字符、retrieve-agent >15000 字符为常态）时抛 `NameError: name 'task' is not defined` → 外层 catch 吞掉 → 用户收到 `An error occurred: name 'task' is not defined`，回答/引用/保存全部丢失，错误文本还被存进历史与 LTM 提取。
- **根因**: `task` 变量只在 `_execute_tool_call` 作用域存在，`supervisor_node` 内从未定义：
  ```python
  truncated += f"\n\n[... 已截断, 完整内容已保存至 {task.get('output_path', '文件')} ...]"
  ```
- **影响**: "写长文/长检索"场景高频触发 → expert 主流程大面积不可用。
- **修复方案**:
  ```python
  # 删除该引用，改为固定文案，并补充截断日志
  truncated += "\n\n[... 已截断 ...]"
  logger.warning(f"[ExpertGraph] truncated {agent_display} result {len(result_text)} chars > cap {cap}")
  ```
  若需保留路径提示，改用 `tc_dict["args"].get("output_path", "文件")`。
- **验证方法**: 构造 >15000 字符 retrieve 结果发起请求，断言正常返回答案；日志出现 truncated warning 且无 NameError。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 源码无 `task.get('output_path')` 引用、截断分支含 warning 日志、语法通过；回归脚本 20/20 PASS

## AG-2 [P0] write-agent 双重写入：文件内容重复 / 被成功消息污染 + write 语义被改 append
- **位置**: `agent/src/graph/expert_graph.py:271-278`（direct-write）、`289-299`（forced save）、`agent/src/tools/file_ops.py:32-44`
- **现象**: ① 常规"保存综述"流程：子 Agent 已按提示调用 `write_local_file` 写入文档 A → supervisor forced-save 再以 append 写入同一文件 → **文件 = A + "\n\n" + A**；② direct-write 路径把 `"Success: write to ..."` 成功消息当正文追加进文件。
- **根因**:
  ```python
  # expert_graph.py:289-299 —— 假定子Agent没保存，实际它必保存（prompt: MUST call write_local_file）
  if output_path and answer_text and len(answer_text) > 50:
      save_msg = write_local_file.func(output_path, answer_text)   # 文件已存在 → 强制追加
  # file_ops.py:32 —— write 模式遇已存在文件也强制追加，语义上永不覆盖
  if mode == "write" and target_path.exists():
      content = existing + "\n\n" + content
  ```
- **影响**: 所有"保存/生成报告"型任务产出文件结构性重复；写第二版时新旧叠加 → 核心功能产出损坏文档。
- **修复方案**:
  1. `run_agent` 返回值增加 `file_saved: bool`（由 write-agent 的工具调用记录）；supervisor forced-save 条件改为 `if not file_saved` 才兜底写入；
  2. direct-write 分支 result 不再复用 `save_msg`，改为短文案 `f"已保存到 {save_path}"`（<50 字符不触发二次写）；
  3. `file_ops.write_local_file` 恢复 "write=覆盖" 语义（去掉强制追加防御）。
- **验证方法**: 全流程回归 `POST /api/v2/chat {"query":"写一份黄龙病综述并保存到 hlb.md"}` → 文件只含一份正文、无 "Success:" 字样；二次"更新 hlb.md" → 旧内容被替换而非叠加。
- **状态** (2026-08-11): ✅ 已修复 — 验证: write=覆盖 / append=追加 / 越权路径拒绝 3 项 PASS（回归脚本）；`file_saved` 标记经 agent_runner→expert_graph 联动

## AG-3 [P3] ContextBudget 阈值三处分散不一致 + 无早期压缩 → 压缩机制在真实使用中永远静默
- **严重度修正说明（2026-08-11）**: 原判断 P1"max_tokens=1000000 远超模型窗口 → 长会话必然超窗"基于 deepseek-chat 64K/128K 假设；实际使用 **DeepSeek V4 Flash（上下文 1M tokens）**，`max_tokens=1000000` 与模型窗口对齐，**不构成超窗风险**。前端"剩余 999,xxx tokens"为真实剩余量。原 P1 作废，降级为 P3。
- **位置**: `agent/src/graph/expert_graph.py:326-330`、`light_graph.py:66-70`（硬编码 `1000000 / 0.85 / 0.93`）、`agent/config.yaml:193-200`（`0.90/0.95`）、`guardrails/circuit_breaker.py` 配置（`90/95`）
- **现象**: ① 三处阈值配置不一致：改 `config.yaml` 不生效（图内硬编码覆盖）；② 压缩触发需 850K tokens（≈170+ 轮），真实会话几乎永不触发 → 压缩机制虽正确对齐窗口但实际是静默死代码。
- **根因**: 图内硬编码覆盖 config；压缩阈值只按"窗口溢出"设计，未考虑 1M 窗口下每轮请求全量历史导致**延迟与成本随轮次线性增长**（50 轮时单请求 ~250K+ tokens，延迟数十秒级）。
- **影响**: 配置运维被误导（DEP-5 已列）；长会话延迟/成本随轮次线性增长无兜底（用户确认真实会话偏长，>50 轮场景存在）。
- **修复方案**（2026-08-11 已确认纳入早期压缩）:
  ```yaml
  # config.yaml —— 保留 1,000,000（对齐模型窗口），做配置单向化 + 早期压缩
  context_budget:
    enabled: true
    max_tokens: 1000000
    soft_threshold: 0.60        # 早期压缩：60% 窗口（~600K tokens，约 60-150 轮）触发摘要
    hard_threshold: 0.93        # 硬截断兜底不变
  ```
  ```python
  # expert_graph.py / light_graph.py：删除硬编码 ContextBudgetConfig(...)，
  #   改读 settings.CONTEXT_BUDGET_MAX_TOKENS / SOFT / HARD（config.py 新增字段）
  # circuit_breaker.py：删除 90/95 重复阈值（该模块本就无实例化，属死配置）
  # 压缩 LLM 使用 main 模型（非 FAST_MODEL）：触发频率低，质量优先于成本
  # 必须联动 AG-6（摘要持久化）一并实施，否则每轮重复压缩反而增加成本
  ```
- **验证方法**: synthetic 500 轮历史 `budget.check(msgs)` 断言触发 SUMMARIZE/TRUNCATE；改 config 阈值后图内行为随之变化（不再被硬编码覆盖）；真实长会话（40+ 轮）`context_status.compressed: true` 且后续每轮输入 token 回落至摘要量级。
- **状态** (2026-08-11): ✅ 已修复 — 验证: settings/config.yaml 一致（1M/0.60/0.93）；20 轮 NORMAL / 400 轮 SUMMARIZE / 900 轮 TRUNCATE 全部命中；circuit_breaker 重复阈值已删；压缩 LLM 已切换 main 模型

## AG-4 [P2] `session_id="new"` 被当字面值处理，chat 流程从不注册会话
- **位置**: `agent/src/api/main.py:102`、`manager.py:99-107`
- **现象**: 客户端按约定传 `session_id="new"` 期望开新会话时，`sid = req.session_id or uuid4()` 将其原样入库，所有传 `"new"` 的请求共享同一份历史；chat 流程全程未调用 `get_or_create_session`，会话行靠 sqlite 外键默认关闭才"侥幸"不报错。
- **根因**: `"new"` 语义只在 `/api/v1/session/new` 生效；`chat_v2` 未复用。
- **影响**: 前端"新会话"在部分调用路径下退化为共享会话；后续开启外键或迁移 schema 直接抛 FK 异常。
- **修复方案**: `chat_v2` 入口改为 `sid = await session_manager.get_or_create_session(req.session_id)`（一行，内部已处理 `"new"`/空值）。
- **验证方法**: 连续两次 `POST {"session_id":"new"}` → 第二次回复不带第一次上下文；sessions 表为两个不同 UUID。
- **状态** (2026-08-11): ✅ 已修复 — 验证: "new"/None 生成 UUID、已有 id 原样返回；chat_v2 统一走 get_or_create_session

## AG-5 [P2] 长期记忆：全局表无会话归属、无衰减、无冲突处理，注入等权
- **位置**: `agent/src/guardrails/memory.py:116-141`（`save_long_term_fact`）、`expert_graph.py:677-689`、`light_graph.py:329-340`、`agent/src/core/context_manager.py:109-116, 203-207`
- **现象**: 任何会话产生的核心事实（含草稿/临时结论）无条件写入全局表，此后所有会话都召回并注入；同义不同措辞即重复（自然语句作 PK）；与新鲜检索结果冲突时 LLM 无裁决规则；`recall_long_term_memory` 无长度上限；每次请求同步执行最多 500 条 fact 的向量召回（阻塞事件循环）。
- **根因**: `save_long_term_fact` 签名无 `session_id`；`INSERT OR REPLACE` 以长句为 key；无 TTL/衰减；注入块无来源标注、无权重规则。
- **影响**: 跨会话知识污染 → 回答被陈旧/错误事实带偏；轻量级事实稀释 top-5 有效位；每次请求额外 1-3s 阻塞延迟。
- **修复方案**:
  1. 表加 `owner_session`, `source_query` 列；`recall` 按 owner 过滤，注入块标注来源与更新时间；
  2. TTL/衰减：`confidence *= 0.95/day`，30 天过期清理；
  3. key 归一化（实体+谓词抽取）或加 `fact_key` 合并冲突规则（保留置信度高者）；
  4. `recall_long_term_memory` 增加 `max_chars=1500` 截断；改 `asyncio.to_thread` 调用避免阻塞事件循环；
  5. system prompt 规则："long_term_memory 仅作提示，若与本次检索证据冲突，以检索证据为准并明示差异"。
- **验证方法**: 会话 A 存过期事实，会话 B 检索到相反证据 → B 的回答引用新证据并明示冲突；写入 31 天后不再召回。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 表迁移含新列；save 带 `owner_session/source_query`；recall 置信度 0.95/天衰减 + 来源/时间标注 + `max_chars` 截断；save 节点 `to_thread` 异步化；constraints.md 新增冲突规则

## AG-6 [P3] 压缩摘要永不持久化 → 摘要状态无法跨请求复用
- **严重度修正说明（2026-08-11）**: 原 P2 含"历史无限增长导致超窗"理由，1M 窗口下不成立；压缩触发本就需 850K tokens，此问题仅在 AG-3 可选方案（早期压缩）落地后才有实际触发场景，降级 P3 并保持记录。**2026-08-11 二次确认：AG-3 已确认纳入早期压缩（soft=0.6），本项必须与 AG-3 一并实施，作为其持久化依赖。**
- **位置**: `agent/src/core/context_manager.py:96-107`、`session/manager.py:174-229`
- **现象**: `ctx.history_summary` 仅存活于本次请求内存；DB 原始消息原样保留；压缩若触发，每轮请求重复压缩（成本×2），且无法支持重启后恢复压缩态。
- **根因**: `budget.check()` 结果（`result.messages`/`summary`）未写回 DB。
- **影响**: 摘要机制不完整；若采纳 AG-3 早期压缩方案，此问题会随触发频率放大。
- **修复方案**: `ContextManager.load` 中当 `result.level != NORMAL` 时异步执行 `session_manager.replace_history(session_id, result.messages)`（新增：事务内 DELETE + INSERT，或按 turn 标记）；摘要以独立 `summaries` 表存储，加载时优先读摘要。
- **验证方法**: 触发压缩后 `SELECT COUNT(*) FROM messages` 下降；二次请求不再产生 Compactor 日志。
- **状态** (2026-08-11): ✅ 已修复 — 验证: `replace_history` 事务内 DELETE+INSERT，10 条→4 条、内容正确（回归脚本 PASS）；ContextManager.load 压缩后自动持久化

## AG-7 [P2] 工具执行无整体超时、子 Agent LLM 调用无重试 → 挂死或静默降级
- **位置**: `agent/src/tools/registry.py:134-145`、`agent/src/core/agent_runner.py:117-121`
- **现象**: 无超时工具卡住时 SSE 流永久 heartbeat；子 Agent LLM 调用失败直接 `break`，返回 `(no output)` 冒充成功回传 supervisor。
- **根因**:
  ```python
  content, artifact = await asyncio.get_running_loop().run_in_executor(None, lambda: raw_func(**args))
  # 无 asyncio.wait_for；agent_runner 无重试
  except Exception as e:
      logger.error(...); break
  ```
- **影响**: 单工具挂死拖垮整条 SSE 连接；瞬时网络错误被静默吞掉，产出低质"无输出"结果。
- **修复方案**: `_run_single_tool` 外包 `asyncio.wait_for(..., timeout=settings.TOOL_EXEC_TIMEOUT_SEC or 60)`，超时返回 `[ERR_TIMEOUT]` ToolMessage；`agent_runner` 增加 3 次重试（backoff 2s），耗尽后返回显式 `[AgentError]` 前缀结果。
- **验证方法**: mock 工具 sleep 120s → 60s 内收到 `[ERR_TIMEOUT]`；断网模拟下 retrieve-agent 返回错误串而非 `(no output)`。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 慢工具 1s 超时返回 `[ERR_TIMEOUT]`（模拟工具实测）；agent_runner 3 次重试 + `[AgentError]` 前缀（AG-14 联动）

## AG-8 [P2] 子 Agent 工具结果无分档截断 + `context[:24000]` 硬切证据
- **位置**: `agent/src/core/agent_runner.py:176-200`、`88`
- **现象**: supervisor 侧有 `TOOL_RESULT_CAPS` 分档，子 Agent 内部把每次工具返回原样 append（可超 60K 字符）；`context[:24000]` 把 `_all_retrieved` 与 goal 合并后任意切尾，write-agent 依据残缺证据写作。
- **根因**: 两层截断策略不一致；证据截断无优先级策略。
- **影响**: 子 Agent 上下文膨胀抬高每轮延迟与成本（1M 窗口下不会超窗，但 60K+ 字符/次无谓消耗）；综述引用缺失来源段落，编造风险上升。
- **修复方案**: `agent_runner` 复用 `settings.TOOL_RESULT_CAPS` 截断 ToolMessage（保留 `[...已截断]` 标记）；`context` 截断改为"每篇 abstract 限 800 字符 × 保留前 N 篇"。
- **验证方法**: 日志断言子 Agent 内 ToolMessage ≤ cap；生成综述的引用条目均存在于截断后上下文。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 子 Agent 工具结果按 TOOL_RESULT_CAPS 分档截断；context 按完整条目截断不切尾（单测 4 项 PASS）

## AG-9 [P2] `write_local_file` 无并发锁、无原子写
- **位置**: `agent/src/tools/file_ops.py:13-48`
- **现象**: 并发两个写请求对同一文件 read-modify-write 竞态 → 丢失更新；直接 `write_text` 非原子，崩溃产生半截文件。
- **根因**: `target_path.write_text(content)` 直写；无 `os.replace`；无锁。
- **修复方案**:
  ```python
  tmp = target_path.with_suffix(target_path.suffix + ".tmp")
  tmp.write_text(content, encoding="utf-8")
  os.replace(tmp, target_path)          # 原子替换
  # 进程内按路径哈希分桶 threading.Lock；跨进程用 filelock
  ```
- **验证方法**: 并发 10 任务写同一文件，最终内容完整且为最后一次写入；kill 写入中途进程，目标文件无半截内容。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 6 线程并发写最终内容完整单份、无 `.tmp` 残留（单测 PASS）

## AG-10 [P2] `statistical_analysis` 无文件大小/样本量上限、`pdf_read` 无大小检查 → OOM 风险
- **位置**: `agent/src/tools/analyze.py:49-52`、`agent/src/tools/search.py:509-517`
- **现象**: `pd.read_csv` 全量读入内存；`config.file_io.read_max_size_mb: 50` 与 `statistics.max_sample_size: 100000` 在此未用；`pdf_read` 直接 `fitz.open` 大文件逐页全提取。
- **根因**: 配置定义了防护参数但工具实现未接线。
- **修复方案**: 两处读取前统一 `size_mb > settings.FILE_READ_MAX_SIZE_MB → [ERR_FILE_TOO_LARGE]`；`pd.read_csv(..., nrows=settings.STATS_MAX_SAMPLE_SIZE)` 或先统计行数限流，结果头标注截断。
- **验证方法**: 60MB CSV → 返回 `[ERR_FILE_TOO_LARGE]`；100 万行 CSV → 结果标注 `n=100000 (截断)`。
- **状态** (2026-08-11): ✅ 已修复 — 验证: 15 万行 CSV 标注 `[数据截断]`；51MB 文件返回 `[ERR_FILE_TOO_LARGE]`；pdf_read 同步接线（单测 PASS）

## AG-11 [P2] `_idx_map` miss 时索引回退 `-1` → 静默返回语料库最后一条 chunk
- **位置**: `agent/src/retrieval/multi_retriever.py:207-215`（`_search_qdrant`）
- **现象**: Qdrant 点 id 与 `(batch, chunk_index)` 映射不一致时命中 -1 → `self.global_chunks[-1]` 返回语料库最后一条 chunk，带真实 score 进入 RRF/重排，可能被引用进回答。
- **根因**:
  ```python
  return [(self.global_chunks[self._idx_map.get((batch_name, p.id), -1)]["_global_idx"], p.score) ...]
  ```
- **影响**: 检索结果间歇性混入完全无关文本 → 回答引用错乱；难以复现排查。
- **修复方案**:
  ```python
  for p in res.points:
      g = self._idx_map.get((batch_name, p.id))
      if g is None:
          logger.warning(f"[Retriever] unmapped point {p.id} in {batch_name} — skipped")
          continue
      out.append((g, p.score))
  ```
  并在 `_load_data` 后做映射完整性自检（`count_points == len(_idx_map)`）。
- **验证方法**: 单测构造 `_idx_map` 缺项 → 该项被跳过而非返回 global_chunks[-1]。
- **状态** (2026-08-11): ✅ 已修复，**发现并修复 P0 级真实检索错乱** — 真实数据 Qdrant point id 为随机整数、`chunk_index` 为论文内编号，原 `(batch, chunk_index)` 键匹配率 ~0% → 检索恒返回 `global_chunks[-1]`（语料库最后一条）；已改为 payload `(paper_id, chunk_index)` 键，真实数据抽样 5 批次×300 点匹配率 **100%**；自检改为抽样匹配率告警；**端到端验证 PASS**：3 个真实查询 × 10 结果 = 30 命中、0 条指向 last chunk、24 篇去重文献语义贴合（模型经清华镜像补装）

## AG-12 [P2] Light/Expert 路由配置全部死代码，边界 case 零覆盖
- **位置**: `agent/config.yaml:122-128`（`route.*`）、`agent/src/graph/graph.py:20-24`
- **现象**: `route.enabled/simple_keywords/complex_keywords/llm_judge_on_uncertain` 无任何引用；服务端仅按 `light_mode` 布尔分发。"短 query 高复杂度"（如"写综述"）边界 case 无兜底。
- **根因**: 路由层被简化删除但配置未清理。
- **影响**: 用户误切 light 模式 → 综述类请求被限制在 2 轮、单工具内，产出质量骤降且无服务端干预。
- **修复方案**: 实现 `route` 判断（query 长度 + 关键词命中 → expert；`llm_judge_on_uncertain` 用 fast model 单次分类），`light_mode=false` 且命中复杂关键词时强制 expert，并输出 `route=expert reason=...` 日志；或删除死配置并文档化"路由由前端负责"。
- **验证方法**: `light_mode:true` 提交"写一篇柑橘黄龙病综述并保存"→ 日志 `route=expert reason=complex_keyword`。
- **状态** (2026-08-11): ✅ 已修复 — 验证: light+复杂关键词→expert / light+长句(>25字)→expert / 短问→light / 简单关键词→light（单测 5 项 PASS）；日志输出 `route=... reason=...`

## AG-13 [P2] rerank 阈值组合偏激进 → 空结果率偏高、检索被静默"兜底"
- **位置**: `agent/config.yaml:13-14`、`agent/src/retrieval/multi_retriever.py:254-260, 335-341`
- **现象**: `rerank_threshold: 0.25` + `dynamic_threshold_ratio: 0.60`；bge-reranker-v2-m3 sigmoid 分数分布偏保守，top1=0.2 时全量过滤 → `return []` → 回答退化为纯模型知识、零引用，用户无感知。
- **根因**: 绝对阈值未按实际分数分布校准；无最低保留数兜底。
- **修复方案**: ① 用 `logs/retrieval/*.log` CE 分数分布校准（经验 0.10-0.15）；② 增加 `min_keep: 3`（passed 为空但 reranked 非空时保留 top3 并 WARNING）；③ 配置注释标注校准依据。
- **验证方法**: 回放一周检索日志，空结果率下降且低质量拦截样本仍在。

## AG-14 [P2] 关键路径静默失败：load 失败无感降级、子 Agent 错误冒充正常结果
- **位置**: `agent/src/graph/expert_graph.py:376-379`、`agent_runner.py:117-121`、`main.py:256-261`
- **现象**: ① 会话 DB 读取失败 → load node 返回 `"unavailable"` 但 supervisor 照常运行 → 用户历史上下文静默消失，无任何提示；② 子 Agent LLM 异常返回 `(no output)` 冒充正常（见 AG-7）；③ error 事件内容为裸 `str(e)`。
- **根因**: `except Exception` 后仅 `logger.warning` + 默认值兜底，未把降级事实传递到体验层。
- **修复方案**: load 失败时 SSE 推送 `{"event":"context_degraded","reason":"history_unavailable"}`；`agent_runner` 失败返回 `[AgentError]` 前缀；error 事件加 `code` 字段。
- **验证方法**: 临时改名 sessions.db → SSE 流出现 `context_degraded`；前端显示警示而非静默。
- **状态** (2026-08-11): ✅ 已修复 — 验证: load 失败推送 `context_degraded` 事件；agent_runner LLM 失败返回 `[AgentError]` 前缀（与 AG-7 联动）

## AG-15 [P3] Token 估算失真（chars/1.5 系数）
- **位置**: `agent/src/core/context_budget.py:50-72`
- **现象**: 对英文高估约 2.7 倍、对中文低估约 30%；不含 system/工具 schema/当前 query；前端展示与后端同源同失真。
- **修复方案**: 接入真实 tokenizer（tiktoken/模型 tokenizer，缓存消息哈希）；次选按语言区分系数（中文字符×1.1 + 英文单词×1.3）。
- **验证方法**: 同一混合文本 `estimate_tokens` 与真实 token 数偏差 < 15%。

## AG-16 [P3] 策略卡片指引调用不存在的工具（web_search / multi_search）
- **位置**: `agent/src/core/strategy_cards.py:14, 17, 41, 35`
- **现象**: 命中检索卡片时 system prompt 指示使用 `web_search`/`multi_search`，但 supervisor 仅绑定 4 个工具 → 模型行为漂移。
- **修复方案**: 卡片文案改为实际工具名（`citrus_rag_search`/`academic_search`/`encyclopedia_search`）。
- **验证方法**: 检索类 query 命中卡片时，LLM 工具调用名 ⊆ 绑定集合。

---

# 修复优先级排序表

| 优先级 | 编号 | 问题 | 工时(人时) |
|---|---|---|---|
| **P0** | AG-1 | supervisor `task` NameError 崩溃 | 0.5 |
| **P0** | AG-2 | write-agent 双写污染（含 write 语义修复） | 2 |
| **P2** | AG-7 | 工具整体超时 + 子 Agent LLM 重试 | 2 |
| **P2** | AG-4 | session "new" 语义统一 | 0.5 |
| **P2** | AG-5 | LTM 归属/衰减/冲突规则 + 注入标注 | 3 |
| **P2** | AG-8 | 子 Agent 工具结果分档截断 + 证据截断策略 | 1 |
| **P2** | AG-9 | 原子写 + 并发锁 | 2 |
| **P2** | AG-10 | 文件大小/样本量上限接线 | 1 |
| **P2** | AG-11 | `_idx_map` miss 跳过 + 启动自检 | 1 |
| **P2** | AG-12 | 服务端路由兜底（或删配置声明） | 2 |
| **P2** | AG-13 | rerank 阈值校准 + min_keep 兜底 | 2 |
| **P2** | AG-14 | 静默失败显性化 | 1 |
| **P3** | AG-15 | token 精确计数 | 2 |
| **P3** | AG-16 | 卡片文案对齐工具清单 | 0.5 |
| **P3** | AG-3 | ContextBudget 配置单向化 + 早期压缩（soft=0.6，压缩用 main 模型） | 3 |
| **P3** | AG-6 | 压缩摘要持久化（与 AG-3 一并实施，缺一不可） | 1 |
| — | DEP-1~6 | 部署/生产级问题（存档不实施） | 0（挂账） |

**功能侧合计 ≈ 24.5 人时**（P0 共 2.5 人时优先处理；P2 共 15.5 人时；P3 共 6.5 人时）。

---

# 第三章 Agent 机制框架对照（筛选结论）

目标约束：**优化 Agent 的调用、推理、执行更顺畅、更智能**。以下按此目标严格筛选补充项，非通用工程完备性。

## 3.1 必要补充（直接提升三要素，本轮做）

### N1. Prompt 层重构（推理 + 调用，最高杠杆）——【本次新增发现】
`prompts/system/` 与 `prompts/agents/` 检查结论：**推理机制缺失一半，且有 4 处 prompt 与实现矛盾（真实调用错乱源）**：

| # | 位置 | 问题 |
|---|---|---|
| P-A | `decision_guide.md:12` | 保存规则写成"调用 call_write_agent **或 read_local_file**"——读工具被列为写方案，模型可能调 read 来"保存" |
| P-B | `decision_guide.md:27` | "read_local_file 读取文件全部内容" vs `readfile.py` 实际 PDF 默认截断 30000 字符——prompt 误导模型期待全文 |
| P-C | `retrieve-agent.md:10-11` | 要求"使用联网搜索/百科搜索"，但 `agent_runner.py:280-290` 只绑定 `citrus_rag_search + academic_search`——子 Agent 被指引调用不存在的工具（AG-16 同类，更内层） |
| P-D | `write-agent.md:28` | "单次输出约 4000-5000 字" vs 实际 `max_tokens=32768`——分块策略基于旧限制，白费多轮调用 |

**缺失项**：
- 无 CoT 强制外显：`decision_guide.md` 只有行为规则表，无"调用前先分析目标/已有信息/缺口/备选方案"流程指令 → 模型跳步直接猜工具
- 仅 write-agent 有"最终自检"，supervisor / retrieve / analyze 均无反思指令
- **修复方案**：在 `constraints.md` 或 `decision_guide.md` 追加工具调用前三问模板；修正 P-A~P-D 四处矛盾；为 supervisor/retrieve-agent 补充失败反思指令
- **验证方法**：固定问题集对比修正前后工具调用名分布与首次调用正确率；`thinking` 事件出现率提升
- **状态** (2026-08-11): ✅ 已修正 — P-A（保存规则不再列 read_local_file）、P-B（read 描述对齐 PDF 30000 字符截断）、P-C（retrieve-agent 移除 web/百科指引）、P-D（输出限制更新为 32768 tokens）；`decision_guide.md` 新增"工具调用前思考"CoT 四问

### N2. 工具 schema / description 统一评审（调用准确率）
- 与 N1 矛盾修正一并做：description 走"动词+对象+边界+反例"规范（`read_local_file` 的 description 为正例）
- 错误返回统一 `[ERR_类别]+可操作提示` 格式（LLM 可直接读错因修正）
- 范围：`expert_graph.py:35-163`（`_AGENT_TOOLS`）、`strategy_cards.py` 文案、三个子 Agent prompt
- 验证方法：构造误用场景集（如"保存到 output/1.md"、"读全文"），断言调用工具正确

### N3. 失败分层降级（执行顺畅）
- 即文章"失败分层矩阵"，落本项目 = AG-7（子 Agent 无重试/工具无超时）+ AG-14（异常输出错误文本而非降级回答）+ AG-1（NameError 整轮崩溃）
- 验证方法：断网模拟 / mock 工具 sleep，断言 60s 内收到 `[ERR_TIMEOUT]`；异常时返回降级回答而非裸错误串

### N4. 上下文工程生效（推理质量）
- AG-3（配置单向化 + 可选早期压缩控延迟/成本）+ AG-6（摘要持久化）+ AG-8（子 Agent 截断）
- 验证方法：见 AG-3/AG-6/AG-8 各自验证

### N5. 模型路由矩阵（低成本顺带项）
- 写"节点 → 模型 → 温度"表（已有雏形：FAST_MODEL 管 suggestions/format_hint/压缩/HyDE，MAIN 管回答）
- 顺带暴露真实问题：`memory.py:238-243` 的 `extract_key_facts` 用同步 OpenAI 客户端 + 15s 超时在异步图节点内跑 → **阻塞事件循环**，需改 `asyncio.to_thread`
- 验证方法：路由矩阵文档化后，逐节点核对实际调用参数与表一致

## 3.2 没必要补充（明确排除 + 理由）

| 维度 | 排除理由（对齐"调用/推理/执行"目标） |
|---|---|
| 真流式改造（LLM stream + tool delta） | 只改善"体验"，不改善调用/推理/执行；需 invoke→stream 全链路重写 + accumulator 接线 + 中断语义，成本最高收益最低。现阶段假流式可接受，远期再评估 |
| 安全治理/鉴权/CORS/沙箱 | 已明确部署级不处理（DEP 章节存档） |
| 并发隔离理论化（单独成文档） | 已审计，是"修复前提"非"智能优化"；按 DEP-3 修即可 |
| Golden-trace 评估回放 | 重基建；当前阶段先修正确性。轻量替代 = 检索日志 + 指标埋点（AG-13 校准顺带） |
| 全局队列 → contextvars 改造 | 已知问题；且 contextvars 在 `asyncio.create_task` 不自动传播（需 `copy_context()`），非本阶段重点 |

## 3.3 待决策项

1. **plan 工作记忆（plan.md 落盘）**：前端已传 `plan_mode` 但后端丢弃（`index.html:1416` vs `main.py:93-96` ChatRequest）。属新功能而非优化现有。建议本轮不做，P0/P1 清完再评估。
2. **评估指标埋点（轻量版）**：done 事件 + `tokens_used` / 工具调用次数 / 空结果率计数。建议随 AG-13 一并做。

## 3.4 落地顺序

```
第一批（P0 + N1 优先）：AG-1 崩溃修复 → AG-2 双写 → prompt 层矛盾修正（N1，纯文本改动，风险最低收益最快）
第二批：N3 失败降级 → N4 上下文预算生效 → N2 工具 schema 统一
第三批：N5 模型路由矩阵 + 指标埋点
```
