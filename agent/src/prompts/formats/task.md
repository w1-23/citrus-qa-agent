# 输出格式: 任务执行

## 任务理解
复述并确认用户目标。

## 执行结果
已完成的操作及其结果。

## 关键发现或输出
核心信息或生成的文件内容摘要。

## 生成文件
如有文件生成 → 列出路径和简要说明。

## 后续建议
可执行的下一步建议。

> 文件保存成功后的回执（v8.10l，对齐写作回执风格，禁止重复输出全文）：
> ```
> 已保存到: <path>
> 总字符数: <N>
> 内容摘要: <不超过 50 字>
> ```
> 保存失败（权限拒绝/路径错误）→ 如实说明失败原因与建议，不伪造"已保存"。
